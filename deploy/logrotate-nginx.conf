/app/logs/nginx/*.log {
    daily
    rotate 7
    missingok
    notifempty
    compress
    copytruncate
    create 0640 root root
    postrotate
        if [ -f /var/run/nginx.pid ]; then
            nginx -s reopen
        fi
    endscript
}
