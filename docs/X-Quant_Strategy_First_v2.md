# X-Quant：本地量化策略优先设计 v2（完整阅读版）

> 日期：2026-09-08。核心交付：量化策略规则、预测任务、策略管理与 Codex 实施规范。
> 第一实现策略：`xq.srpa.breakout_retest.long@0.1.0`。
> 当前证据：源码/文本核查与规格完成；尚未实现引擎或进行真实收益验证。

## 文档结构

| 篇章 | 重点 |
|---|---|
| 第一篇 | 用户指定 GitHub 源码提取、风险与统计口径、社区参考与来源 |
| 第二篇 | 确定性的入场、退出、仓位、预测、回测、消融和晋级条件 |
| 第三篇 | 策略资产、版本/参数/实验/部署/复盘，本地架构、数据模型与接口 |
| 第四篇 | Codex 启动指令、P0–P5、36 项必经验收 |
| 第五篇 | 本次实际完成的检查及尚未验证的边界 |

配套 ZIP 另含分篇 Markdown、主策略 YAML、10 项策略研究目录、交付检查脚本和校验结果。
将分篇文件整体放进项目 `docs/strategy_v2/` 后，把第四篇的启动指令交给 Codex。
旧 X-Stock 文档中“先网站后策略”的实施顺序由本版本替代。

---

## 第一篇：指定仓库与社区策略来源审查

*原分篇文件：`01_SOURCE_AUDIT.md`。*


> 核查日期：2026-09-08。本文审查公开源码与策略文本，不是策略收益认证。  
> 本次已阅读两个指定仓库的核心策略路由、价格行为文本、支撑阻力检测、概率模型和标签/评测逻辑；另外核查 vn.py、Qlib、动量原始资料及相关验证工具。  
> 本次未在本地安装或完整执行上游仓库，未获取用户实际行情，未复现上游收益或预测指标。远端 `main` 是可变引用，部分检索快照可能不同步；未可靠取得固定提交 SHA。实施 P0 必须重新固定 SHA 并核对本文映射，禁止把本文视为固定版本全量审计。

### 1. 结论：提取策略资产，而不是拼装两个网站

PA_Agent 提供的主要是**价格行为策略知识、市场诊断路由、结构特征与决策校验**；支撑阻力仓库提供的主要是**关键位计算、事件标签与统计预测模型**。两者互补，但均不能直接充当本项目完整的持仓与实盘执行系统。[R01–R14]

本项目将这些资产拆成五类管理：`feature`、`regime_detector`、`setup`、`forecast_model`、`risk_policy`。随后组装为完整的 `strategy_version`。一个指标、一个提示词、一个预测器或一段股票分析文字，均不自动算作已完成的交易策略。

策略证据等级统一为：

| 等级 | 含义 | 本次交付能确认到哪里 |
|---|---|---|
| E0 | 找到来源 | 已有来源目录 |
| E1 | 已读关键逻辑并形成规格 | 核心资产达到；本文明确各自边界 |
| E2 | 本项目单元测试、因果性和状态回放通过 | 尚未达到，交给 Codex 实施 |
| E3 | 本项目有成本回测与独立样本外证据 | 尚未达到 |
| E4 | 仿真/影子运行与交易约束验证通过 | 尚未达到 |
| E5 | 有可审计的真实成交记录 | 尚未达到；也不等于未来有效 |

**“原仓库带回测”“开源社区使用广”“模型输出 80%”不能替代 E3–E5。**

### 2. PA_Agent：实际提取了什么

#### 2.1 代码路径到策略资产

| 路径 | 实际逻辑 | 本系统映射 |
|---|---|---|
| `pa_agent/orchestrator/two_stage.py` [R02] | 行情校验→第一阶段诊断→选策略文本→第二阶段方案→输出校验/归档；闸门失败可短路 | `ResearchPipeline` 与不可变决策记录，不移植桌面编排 |
| `pa_agent/ai/router.py` [R03] | 通道、尖峰、交易区间等状态映射策略文件，并叠加形态文件 | `RegimeRouter`；生产路由由确定性特征产生 |
| `pa_agent/ai/decision_nodes.py` [R04] | 程序方向特征、部分决策节点和允许的模型覆盖 | 可审计的方向特征；硬风控不允许 LLM 覆盖 |
| `pa_agent/ai/market_features.py` [R05] | 区间位置、K 线重叠、结构枢轴、突破回收/测试、H/L 候选、测量目标 | `PriceActionFeatures`，逐项重新写契约与测试 |
| `prompt_engineering/上涨通道交易策略.txt` [R06] | 顺势回撤、EMA20、H1/H2、旗形和回踩，明确自身不管理持仓 | `pa_h2_pullback`；仓位和退出由本项目新增 |
| `prompt_engineering/震荡区间交易策略.txt` [R07] | 有方向的区间边缘交易；中部与无方向默认等待 | `biased_range_rejection`，不是无条件双边网格 |
| `prompt_engineering/文件18-突破失败与突破测试.txt` [R08] | 区分试探、突破、失败、回测、再次恢复 | `breakout_retest` 有限状态机 |
| `prompt_engineering/文件19-H1H2-L1L2计数.txt` [R09] | 两段回撤与两次入场尝试，计数有重置条件 | 严格双腿状态机，而非突破次数累加 |
| `prompt_engineering/文件17-止损和止盈与仓位管理.txt` [R10] | 下单前价格几何与交易者方程，文本明确禁止持仓管理 | 借鉴结构约束，不继承其全部止损调整规则 |

#### 2.2 保留的交易思想

保留“先判环境、再找形态”；保留趋势中顺势回撤、突破后等待回测、区间中部回避和无清晰机会时等待。保留信号必须解释到具体 K 线与结构位的要求。[R03][R06–R09]

新系统**不以大模型的文字形态诊断作为必须依赖**。日线核心策略离线可运行。LLM 可为候选解释、生成研究假设与复盘提供帮助，但不得制造权威价格、实证胜率或修改硬风控。

#### 2.3 预测逻辑的真实定位

已审阅的两阶段执行路径是：把结构化行情、策略文本及可选经验记录交给语言模型，再校验其结构化输出。下一根 K 线或下一周期的预测属于这一流程中的模型判断，不能因为 JSON 校验通过就当作已校准概率。[R02]

本项目保留两种彼此隔离的记录：`llm_hypothesis` 保存原始模型判断，`statistical_forecast` 保存用冻结数据、明确标签训练并经样本外评测的概率/收益分布。任何展示给用户的百分比必须标注类型、目标、周期与验证状态。

### 3. 支撑阻力仓库：实际提取了什么

#### 3.1 检测器、特征与统计模型

| 资产 | 已核查逻辑 | 集成决定 |
|---|---|---|
| V1 密度检测 [R12] | 旧成交量分箱基线，有跨箱重复累计问题 | 只作为历史对照，不作为生产默认 |
| V2 融合检测 [R12] | ATR 尺度分箱、衰减成交量/收盘密度、枢轴候选、历史触及 | 提取方法，分离位置、强度和概率 |
| V3 融合检测 [R12] | 候选定位与排序拆分；排序主要由历史事件数和陈旧度构成 | 首要参考检测器；必须复验而非照抄效果 |
| 因果枢轴 [R15] | 枢轴发生时间与确认时间分离，仅使用已确认枢轴 | 提升为全系统时间契约 |
| 双概率模型 [R13] | 标准化后的带正则逻辑回归；触及/守住×支撑/阻力分别建模 | 保留任务分解，修正事件条件与评测设计 |
| 事件标签 [R14] | 首次触及、反应/突破、未决及后续价格统计 | 拆成“区域研究标签”和“真实执行交易标签” |
| 走查/对照 [R16] | 截止时点切片；控制输出数量和距离范围 | 保留因果回放、公平对照和消融 |

V3 源码中的默认排序形式可概括为 `log1p(已判定历史事件数) + 0.8 × 陈旧度`。这只是**排序分**。同文件名为 `p_stall` 的字段由若干特征加权得到，并不是自动约束在 0–1 的概率。本项目分别命名为 `sr_structure_score`、`stall_score`，未经校准不得加百分号。[R12]

概率模型的主要特征为：距区域距离/ATR、距离平方、区域宽度/ATR、历史事件数量、陈旧度、量价密度与 ATR/价格。加载器检查特征顺序，模型缺失时可以不输出概率。这些都是值得保留的工程思想。[R13]

#### 3.2 必须修正的统计解释

上游训练脚本把“触及但未判定为守住或突破”的样本排除在守住模型之外，因此该模型实际估计的是：

```text
P(守住 | 已触及，且结果已经判定，决策时特征)
```

而不是对所有已触及事件的守住概率。它不能直接与触及概率相乘后解释为“这笔交易获利概率”。上游按股票代码划分互斥训练/测试池，检验的是一定程度的跨标的泛化，不能替代训练早于测试的时间外验证。[R17]

新系统保留所有**结果窗口已完整结束**的触及样本，把结果分为 `hold / break / timeout`；窗口尚未完整结束属于 `censored`，不能当作失败、未触及或普通超时。详见策略规格中的预测任务定义。

#### 3.3 关键位研究不等于成交回测

标签代码中，部分事件用最高/最低价与收盘价混合判定，同一根同时命中时偏向 `hold`；其收益代理又采用触及棒收盘作为入口。这样的区域研究口径不自动证明用户在看到收盘结果后仍能以该价格成交，也不证明触及棒内的价格顺序可执行。[R14]

本项目默认采用**收盘形成信号、下一交易会话执行参考**，研究日线模式不假设盘中保护单能够成交。另有授权盘中数据与可验证执行条件时，建立独立执行版本，不混合成绩。

### 4. 需要进入实施缺陷清单的事项

以下区分“从代码可观察的事实”和“本项目的处理”。除数学关系外，均未宣称已在上游完整环境复现。

| 编号 | 观察/风险 | 严重度与本项目要求 |
|---|---|---|
| A01 | 策略文本里有主观成功率与模型估计胜率 [R08][R10] | P0：不得作可交易概率；统计模型单独训练 |
| A02 | 风控文本包含 RR>1 时扩止损的规则 [R10] | P0：不继承；止损以结构为依据，风险靠数量控制，不为凑比例扩风险 |
| A03 | 程序 H/L 候选计数不要求每次之间有完整回撤腿 [R05][R09] | P0：新 H2 采用明确状态机，连续上涨不能误识别二次回撤 |
| A04 | 枢轴端点候选与确认枢轴容易混用 [R05][R15] | P0：`occurred_at / confirmed_at` 分开，未确认值不进入交易状态 |
| A05 | PA 的铁丝网一项比较“包络宽度/平均单棒宽度 <0.3” [R05] | P1：合法 OHLC 下该比值至少为 1，此项不可作为有效过滤；独立重写并测试 |
| A06 | SR 的 `p_stall`、`edge` 名称易被当作概率或可实现收益 [R12] | P0：改名并记录量纲，不做虚假百分比展示 |
| A07 | 守住训练排除未决、训练/测试仅按股票划分 [R17] | P0：增加超时类别、成熟窗口与时间外验证 |
| A08 | 区域研究的同棒结果/入口代理不等于实际交易 [R14] | P0：执行引擎独立计算成本、可卖数量、跳空与实际收益 |
| A09 | 走查末端未必留出“等触及+反应”完整窗口 [R14][R16] | P0：按 `label_end_at` 成熟性筛选，而非只减一个 horizon |
| A10 | 未来几何召回计算遇到无同侧预测时跳过某些真值 [R14] | P1：新召回分母包含该侧全部真值；空输出不能提高召回 |
| A11 | 已查防未来测试未覆盖全部正式检测器，部分缺数据分支返回成功 [R18] | P0：正式检测器参数化测试；skip≠pass，无测试覆盖不能晋级 |
| A12 | 上游实验解释中使用测试结果选择因子/分数 [R12] | P1：不是断言所有结果无效；新系统必须留真正未参与选择的最终样本 |
| A13 | 主分支变化、缓存快照可能不一致 | P0：登记 SHA、文件哈希、抓取时间及差异，不能自动跟随远端 main |

A05 的依据是数学关系：同一组合法 K 线的整体最高价减最低价，不小于组内任一单棒的高低差，所以也不小于这些差的平均数。它只指出该条件不可触发，不代表整个市场诊断模块无用。

### 5. 社区策略如何整合，而不是堆指标

| 来源 | 核查到的可用逻辑 | 在本项目的位置 | 不继承的假设 |
|---|---|---|---|
| vn.py `TurtleSignalStrategy` [R19] | 入场/退出通道、ATR 风险与阶梯加仓示例 | `donchian_20_10_long` 简单基线；ATR 风控参考 | 不直接继承双向交易、加仓与固定手数 |
| vn.py `BollChannelStrategy` [R20] | Boll 通道、CCI 方向过滤、ATR 跟踪退出 | 独立挑战者 `boll_cci_long` | 不把原 15 分钟参数直接当日线最优值 |
| Kenneth French 动量构造 [R21] | 按过去表现排名，原构造使用滞后收益、跳过最近月 | `momentum_12_2_long` 月度基线；主策略的 RS 因子另作短周期改写 | 长多改写不等于原多空因子，不假设 A 股同样有效 |
| Qlib Alpha158 [R22] | 多周期价格/量价特征集合 | 预测研究的可选特征组 | 158 个特征不等于 158 个盈利策略 |
| Qlib LightGBM 工作流 [R23] | 配置化特征处理、训练、验证、测试与记录 | 预测挑战者，先与逻辑回归/线性基线比较 | 不直接用示例时间窗、模型参数和成绩 |
| Qlib TopkDropout [R24] | 按得分持有 Top-K、限制每次替换数量 | 月度/周度排名组合的换手控制参考 | 不把分数解释为上涨概率，不照搬风险仓位 |
| Freqtrade 偏差检查 [R25][R26] | 未来数据检查、指标启动长度一致性分析 | 策略管理中的必经测试 | 不引入其数字资产执行假设；“没发现”不等于数学证明 |
| MLflow Registry [R27] | 工件版本、血缘和版本别名 | 策略/模型注册中心的设计参考；可选适配器 | 首版不强制部署单独 Registry 服务 |

**主策略只整合角色互补的模块**：SR 负责位置，PA 负责事件确认，动量负责候选顺序，ATR 负责资金风险。Boll/CCI、Donchian、机器学习作为独立基线或挑战者，经过消融证明有增益后才参与主组合。不得因为“指标更多”而自动提高置信度。

### 6. 许可、代码获取与隔离策略

PA_Agent 许可文件声明 AGPL-3.0-or-later，SR 仓库提供 GPL-3.0 许可；vn.py CTA 模块和 Qlib 提供 MIT 许可。[R28–R31]

本交付不包含复制的上游代码或整篇策略提示词。默认 `reference_only`：依据新规格独立实现，记录借鉴来源。若要实际导入代码，必须检查具体文件及依赖许可、保留所需声明，并确认组合、修改、分发或远程使用所涉及的义务。隔离进程不是规避许可的保证；把实现改写或改名也不能自动推定没有许可义务。

代码获取流程：用户允许的仓库→固定提交→只读源码区→依赖/许可/网络行为检查→合成夹具测试→受限子进程→人工批准。`git clone`、安装依赖、运行安装脚本和执行策略是不同权限。网页提供一个仓库地址，不代表允许它读取本地密钥、持仓、文件系统或发送通知。

### 7. 来源索引

以下均为本次阅读或核查的第一方源码/官方资料。网页可随版本变化；实施时记录最终抓取结果。策略文本中的经验性收益/概率说法不视为经本项目验证的事实。

| 编号 | 来源与链接 |
|---|---|
| R01 | [PA_Agent 仓库](https://github.com/rosemarycox5334-debug/PA_Agent) |
| R02 | [PA 两阶段执行源码](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/pa_agent/orchestrator/two_stage.py) |
| R03 | [PA 策略路由](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/pa_agent/ai/router.py) |
| R04 | [PA 决策节点](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/pa_agent/ai/decision_nodes.py) |
| R05 | [PA 程序结构特征](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/pa_agent/ai/market_features.py) |
| R06 | [上涨通道策略文本](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/prompt_engineering/上涨通道交易策略.txt) |
| R07 | [震荡区间策略文本](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/prompt_engineering/震荡区间交易策略.txt) |
| R08 | [突破失败与突破测试文本](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/prompt_engineering/文件18-突破失败与突破测试.txt) |
| R09 | [H1/H2 与 L1/L2 文本](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/prompt_engineering/文件19-H1H2-L1L2计数.txt) |
| R10 | [下单前止损止盈文本](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/prompt_engineering/文件17-止损和止盈与仓位管理.txt) |
| R11 | [SR 仓库](https://github.com/rosemarycox5334-debug/Detect_support_and_resistance_levels) |
| R12 | [SR 检测器源码](https://raw.githubusercontent.com/rosemarycox5334-debug/Detect_support_and_resistance_levels/main/src/srlab/detectors.py) |
| R13 | [SR 概率模型](https://raw.githubusercontent.com/rosemarycox5334-debug/Detect_support_and_resistance_levels/main/src/srlab/probability.py) |
| R14 | [SR 标签与几何评测](https://raw.githubusercontent.com/rosemarycox5334-debug/Detect_support_and_resistance_levels/main/src/srlab/labeling.py) |
| R15 | [SR 枢轴确认](https://raw.githubusercontent.com/rosemarycox5334-debug/Detect_support_and_resistance_levels/main/src/srlab/pivots.py) |
| R16 | [SR 走查逻辑](https://raw.githubusercontent.com/rosemarycox5334-debug/Detect_support_and_resistance_levels/main/src/srlab/walkforward.py) |
| R17 | [SR 概率训练与切分](https://raw.githubusercontent.com/rosemarycox5334-debug/Detect_support_and_resistance_levels/main/scripts/train_probability.py) |
| R18 | [SR 防泄漏测试](https://raw.githubusercontent.com/rosemarycox5334-debug/Detect_support_and_resistance_levels/main/src/srlab/leakage.py) |
| R19 | [vn.py TurtleSignalStrategy](https://raw.githubusercontent.com/vnpy/vnpy_ctastrategy/main/vnpy_ctastrategy/strategies/turtle_signal_strategy.py) |
| R20 | [vn.py BollChannelStrategy](https://raw.githubusercontent.com/vnpy/vnpy_ctastrategy/main/vnpy_ctastrategy/strategies/boll_channel_strategy.py) |
| R21 | [Kenneth French 月度动量构造](https://mba.tuck.dartmouth.edu/pages/faculty/ken.french/data_library/det_mom_factor.html) |
| R22 | [Qlib Alpha158 特征](https://raw.githubusercontent.com/microsoft/qlib/main/qlib/contrib/data/loader.py) |
| R23 | [Qlib LightGBM/Alpha158 工作流](https://raw.githubusercontent.com/microsoft/qlib/main/examples/benchmarks/LightGBM/workflow_config_lightgbm_Alpha158.yaml) |
| R24 | [Qlib TopkDropout](https://raw.githubusercontent.com/microsoft/qlib/main/qlib/contrib/strategy/signal_strategy.py) |
| R25 | [Freqtrade Lookahead Analysis](https://www.freqtrade.io/en/stable/lookahead-analysis/) |
| R26 | [Freqtrade Recursive Analysis](https://www.freqtrade.io/en/stable/recursive-analysis/) |
| R27 | [MLflow 模型注册中心](https://mlflow.org/docs/latest/ml/model-registry/) |
| R28 | [PA 许可](https://raw.githubusercontent.com/rosemarycox5334-debug/PA_Agent/main/LICENSE) |
| R29 | [SR 许可](https://raw.githubusercontent.com/rosemarycox5334-debug/Detect_support_and_resistance_levels/main/LICENSE) |
| R30 | [vn.py CTA 许可](https://raw.githubusercontent.com/vnpy/vnpy_ctastrategy/main/LICENSE) |
| R31 | [Qlib 许可](https://raw.githubusercontent.com/microsoft/qlib/main/LICENSE) |
| R32 | [scikit-learn 概率校准](https://scikit-learn.org/stable/modules/calibration.html) |
| R33 | [scikit-learn TimeSeriesSplit](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.TimeSeriesSplit.html) |
| R34 | [上交所交易规则：2026 年修订发布页](https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml) |

R34 发布页显示新规则于 2026-07-06 生效，且存在暂缓实施条文。因此实施不能仅按“文档标题为最新”就启用所有条款，必须结合适用品种、生效时间和实施状态，建立有日期的市场规则配置。本文不硬编码一套对所有市场、所有板块、所有年份通用的交易限制。

---

## 第二篇：XQ-SRPA 主策略与预测规则

*原分篇文件：`02_SRPA_STRATEGY_SPEC.md`。*


> SRPA = Support/Resistance + Price Action。  
> 策略族：市场状态路由、支撑阻力定位、价格行为确认、相对强弱排序、独立组合风控。  
> 首个完整实现：`xq.srpa.breakout_retest.long@0.1.0`。  
> 当前状态：**已形成规则规格，未完成本项目收益验证，不得标注“实盘验证通过”**。  
> 本文的参数是待测试起点，不是最优参数或获利保证。所有“买入/卖出”描述均为策略规则；实际模式先采用仿真与人工操作提醒，不自动连接券商。

本文与 `01_SOURCE_AUDIT.md` 配套。`[Rxx]` 是来源审查文档中的编号。来自上游的思想与本项目新增的工程化规则分别标注，避免把新规则说成上游已经证明有效。

### 1. 策略究竟交易什么

策略假设：在可交易、流动性足够且市场没有进入禁入状态时，**具有明确上行结构的股票，突破既有阻力后回测该区域，并再次出现收盘确认**，可能比任意追涨有更清晰的失效位置。相对强弱只决定候选优先级，不替代入场。该假设借鉴 PA 的突破测试与顺势思想、SR 的区域识别，再用独立退出和账户规则补全。[R06][R08][R12]

必须同时回答六个问题：交易谁、什么时候产生信号、什么条件下允许成交、错在哪里、怎样退出、这次风险占账户多少。任何一项缺失时，该对象只属于研究候选，不属于可操作方案。

#### 1.1 范围与模式

| 维度 | v0.1 默认值 |
|---|---|
| 市场 | A 股普通股票作为第一实现假设；市场/板块规则必须配置并按生效日期读取 |
| 方向 | 只做多、无杠杆、无摊平、无金字塔加仓 |
| 决策频率 | 每个交易会话结束后，使用完整且已校验的日线 |
| 执行 | 下一交易会话；先仿真与人工提醒，不承诺以开盘价真实成交 |
| 策略组装 | 先只启用突破回测；H2 和偏多区间反弹通过各自验收后再启用 |
| 预测 | 默认 `rule_only`；统计模型是独立可审批版本，LLM 不在交易必经链路 |
| 数据/权益 | 本地授权快照、点时股票池、公司行为、市场日历、真实成交与现金账本 |

#### 1.2 两种执行口径不能混用

**EOD_ASSIST，默认。** 收盘判断入场、止损、止盈、趋势失效，下一交易会话尝试执行。止损价是决策阈值，不是保证成交价。回测用 `next_open_reference` 作为参考成交模型并计费/滑点；真实运行必须以用户导入的成交为准。只有日线数据时，无法精确验证用户开盘后数分钟才处理通知的成本，应标记延迟风险，并用实际仿真/人工成交差异评估。

**MONITORED_INTRADAY，后续。** 必须具备授权盘中行情、可用的执行/人工监控条件、独立触发器与更细粒度回放。不能用每日最低价假装日内止损总能成交，也不能把 EOD 回测成绩贴到盘中版本。没有券商条件单能力时，本地“虚拟止损”只是提醒。

### 2. 策略资产与组合方式

| ID | 角色 | 默认状态 |
|---|---|---|
| `F-PA` | EMA、ATR、K 线收盘位置、重叠、确认枢轴、突破/回收事件 | 必须实现与测试 |
| `D-SR` | 因果关键位区域检测，参考 V3 的定位/排序分离 | 必须实现与测试 |
| `G-REGIME` | 个股状态及大盘新开仓闸门 | 必须实现与测试 |
| `S-BR` | 突破→保持→回测→收盘确认 | 首个交易策略 |
| `S-H2` | 上行背景中，两次不同回撤腿后的二次确认 | 第二阶段，默认关闭 |
| `S-RG` | 偏多区间的下边缘收盘反弹 | 第三阶段，默认关闭 |
| `F-RS` | 中期相对强弱横截面排序 | 默认作为排序，不作为额外胜率 |
| `P-TOUCH` | 区域未来接触事件预测 | 雷达/研究用途，不是交易获利概率 |
| `P-REACT` | 接触后守住/突破/超时的条件分布 | 研究用途，必须注明条件 |
| `P-TRADE` | 匹配实际执行规则的成本后收益与盈利概率 | 通过时间外验证后才可过滤交易 |
| `R-PORT` | 持仓、现金、风险预算、敞口、交易约束 | 全局优先；不可被策略或 LLM 覆盖 |

同一账户同一股票同一决策时点只允许一个新增净多头计划。多个策略命中时，按固定顺序 `S-BR > S-H2 > S-RG` 选主计划，其他命中保存为解释；不是叠加三份仓位。更换优先级生成新版本。已有仓位的退出由入场绑定的版本负责。

### 3. 时点、数据与计算契约

#### 3.1 统一时间语义

` t ` 表示交易会话索引，不是简单自然日。主序列从旧到新，所有时间戳带时区，存储统一 UTC。策略只看到 `available_at <= decision_at` 且 `is_final=true` 的记录。

每根 K 线至少包含 `instrument_id, session_id, open, high, low, close, volume_shares, turnover_currency, source, received_at, available_at, revision_id`。历史回放与在线恢复回放应区分：前者依据供应商可核实的历史发布时间/版本，后者依据本系统实际收到记录的时间。`received_at` 是本次接收时间，不能伪造为多年前。若只有当前下载的历史数据、无法核实当时版本，可做标为 `VINTAGE_UNVERIFIED` 的探索回测，不能宣称已排除数据修订泄漏。

必须验证 `low <= min(open,close) <= max(open,close) <= high`、价格有限且为正、数量非负、序列有序、不重号。停牌、未上市、未知缺失和供应商漏数是不同状态，不能统一前向填充成可交易价格。

**禁止使用完整数据帧中当前时点之后的行。** 高周期指标只能使用已结束周/月的 K 线；周三不能用周五收盘。跨股票横截面必须采用同一个数据截止时点和缺数政策。

#### 3.2 价格与公司行为

成交、现金、持仓数量、涨跌幅检查使用真实原始价格与当时适用规则。技术计算使用 `decision_at` 当时已知公司行为构成的统一调整口径，保存因子快照，输出区域再映射到该时点可交易价格。

不可把今天下载的整条前复权曲线无条件当作多年前可见数据；也不可声称“后复权”三个字就保证所有供应商修订都是点时安全。拆并股、分红等发生时，冻结结构位按照当时已知映射转换，生成 `level_rebase_event`；不能凭更新后的历史曲线偷偷改变过去信号。无法正确处理的公司行为应阻止新增计划并提示人工核查。

成交量只有与股数单位变化一致时才调整，现金分红不凭空改变股数。技术收益序列与含分红总回报序列分开保存，动量/基准绩效使用明确的总回报口径。

#### 3.3 指标精确定义

| 指标 | v0.1 定义 |
|---|---|
| EMA(n) | 首 n 个有效收盘的均值为种子，之后 `EMA_t = 2/(n+1)*C_t + (1-2/(n+1))*EMA_(t-1)` |
| TR | `max(H-L, abs(H-C_prev), abs(L-C_prev))`；序列首项取 `H-L` |
| ATR(14) | 前 14 个 TR 均值为种子，此后 Wilder 递推 `(13*ATR_prev+TR)/14` |
| CLV | `(C-L)/(H-L)`；零幅 K 线设为 0.5，不能作为强信号棒 |
| body_ratio | `abs(C-O)/(H-L)`；零幅设 0 |
| overlap | 相邻两棒高低区间交集长度/并集长度；零并集视为 1 |
| overlap10 | 最近 10 对已完成相邻 K 线 overlap 均值 |
| ER20 | `abs(C_t-C_(t-20))/sum(abs(diff(C)), 最近20期)`；分母为零设 0 |
| slope20 | `(EMA20_t-EMA20_(t-5))/ATR14_t`，无量纲 |
| RS63 | 个股最近 63 会话总回报减同频基准总回报 |
| RS percentile | 在当日合格证券全集上以 RS63 升序平均名次计算 `(rank-1)/(N-1)`；N<2 不可用 |
| ADV20 | 前 20 会话成交金额均值；用于数量限制的成交股数均值另存为 `ADV20_shares` |

EMA 的递推状态必须从固定数据起点/检查点延续，不能每天任意截断 60 根重算。最低新股观察期 252 会话；实际指标种子可更早。不同启动长度的误差检查参考 Freqtrade 的递归分析思路。[R26]

### 4. 股票池：全市场扫描不等于全市场买入

每日先以点时证券主表构建覆盖全集，再执行用户约束和数据条件。默认排除已退市、当时风险警示、当时不可交易、上市不足 252 会话、缺少关键市场规则的数据。历史上退市的证券仍必须在对应历史股票池与回测中保留。

价格范围与行业列表允许用户配置；未配置时不人为添加价格偏好。默认流动性起点为过去 20 会话平均成交金额不低于 2,000 万元人民币；其他货币必须显式换算或采用该市场的独立阈值。该数值是容量研究参数，不代表高于阈值就安全。

推荐形成过程固定为：

```text
授权覆盖全集 → 点时资格 → 用户价格/行业约束 → 数据和流动性
             → 市场状态 → 入场形态 → 交易几何 → 排序 → 账户风险分配
```

RS 分位在用户价格/行业筛选前的当日合格市场全集上计算。对同股票多形态先按 S-BR、S-H2、S-RG 选主计划；随后整个候选批次按 `RS分位降序、净RR降序、确认时间升序、instrument_id升序` 排序，再逐个分配组合额度。RS 缺失时不参加默认新仓排名并记录原因；不临时用 0 或最高分填补。

没有形态时只进入“观察雷达”，不能为凑推荐数量制造买入。价格/行业信息缺失时输出排除原因，不用零值填补。持仓股票即使不在新开仓池，仍持续执行风险监控。

### 5. 市场状态路由：明确、可重放、允许不交易

以下为本项目工程化起点，不宣称是 PA 原始实现或最优分类器。PA 的状态路由提供结构参考。[R03][R04]

#### 5.1 大盘开仓闸门

`market_gate=RISK_ON` 需要：基准最近有效收盘高于 EMA120，且 EMA20 不低于 5 会话前的 EMA20。否则 `RISK_OFF`，不生成新增买入；缺基准或基准过期则 `UNKNOWN`，同样不新增。

已有仓位不因大盘闸门关闭而被程序虚构成已卖出。其原始退出策略继续执行，组合页显示风险；只有实际成交才能减少账面数量。该大盘过滤必须单独消融，未经验证不得自动增加更多指数条件。

#### 5.2 个股分类，按优先级执行

| 优先级 | 状态 | 条件 | 新增策略 |
|---|---|---|---|
| 1 | `INVALID` | 关键数据、账户、市场规则或公司行为未通过校验 | 无 |
| 2 | `CHAOS` | `overlap10>=0.65` 且 `ER20<=0.20` 且 `abs(slope20)<=0.25` | 无 |
| 3 | `UPTREND` | `C>EMA20>EMA60` 且 `slope20>=0.25` 且 `ER20>=0.25` | S-BR、已批准的 S-H2 |
| 4 | `DOWNTREND` | `C<EMA20<EMA60` 且 `slope20<=-0.25` | 无新多头 |
| 5 | `BIASED_RANGE` | 不满足上列，且 `C>EMA60`、`EMA60_t>=EMA60_(t-5)`、有合格上下区域 | 仅已批准的 S-RG |
| 6 | `NEUTRAL` | 其他 | 无 |

只有大盘 `RISK_ON` 且个股允许时才评估新仓。周线 EMA20 一致性先作为记录特征，不默认增加闸门；后续作为单独挑战者测试。避免多个相似趋势指标叠加到几乎没有交易却未被察觉。

### 6. 支撑阻力引擎：SR-V3-inspired v0.1

#### 6.1 两条实现路径

`upstream_research_adapter` 用于复现固定 SHA 的原仓库，以核对输入/输出与原始口径；须先完成许可检查。

`xq_sr_v01` 是本项目参考 V3 的独立工程化定义，作为默认核心。它与上游 V3 的数值不必相同，因此上游概率权重**不得直接用于此检测器**。选择哪条路径属于策略版本内容，不能运行中静默切换。[R12][R13]

#### 6.2 xq_sr_v01 的确定性算法

使用截至 t 的最近 250 根有效日线，价格统一到 t 的尺度。基础分箱宽度为 `max(price_tick,0.25*ATR_t)`；若超过 4096 箱，按覆盖区间/4096 增大箱宽并向 tick 对齐，记录实际箱宽。

对每根 K 线，将成交股数乘 `2^(-age/60)` 后，按其高低区间与每个箱的重叠长度分摊；零幅棒全部落入最近箱。再把其收盘价作为点观察累计到收盘密度。两条曲线先用 σ=0.5ATR、截断半径 3σ 的离散高斯核平滑；边缘每个输入箱的分配权重重新归一，保持未归一化总量。再分别以自身最大值归一到 0–1，空曲线输出不可用，而不是全市场高质量。成交量守恒检查作用于未归一化量，不对已经无量纲化的分数比较股数。

首版枢轴采用 **k=2 的确认分形**：高点需严格高于左右各两棒高点；低点相反；平手不算枢轴。发生在 i 的枢轴只允许在 i+2 收盘后出现。用已确认高/低点分别构造高/低密度；每个枢轴权重为 `2^(-确认后会话数/120)`，以 σ=0.5ATR 的高斯核累计并按自身最大值归一化。之后可增加带确认时点的 ZigZag 挑战者。[R15]

位置曲线采用 `volume_density + 0.8*close_density + 1.6*pivot_density`。取正值局部峰值，平台相等时取最左峰；按位置分数降序、按中心价稳定排序打破平手；相邻中心至少间隔 0.9 ATR。区域先使用总宽度 0.6 ATR，边界按 tick 向外舍入，每侧最多 3 个。中心低于当前价的是支撑，高于当前价的是阻力；价格在区域内可标注 `CONTACTING`，不得谎称尚未触及。

区域的 `confirmed_pivot_count` 是当前窗口内同侧已确认枢轴价格落在区域边界内的数量。`stall_score=volume_density+0.8*close_density+0.3*区域宽度/ATR`，是可超过 1 的分数，不是概率。

历史事件数只统计**在 t 时点已完整观察后续 10 会话**的非重复接触段，段间隔至少 5 会话；记录 `hold/break/timeout` 全部类别。区域由当前历史推得时，这些属于历史几何描述，不是“过去真实发出的信号”。

`n_resolved=n_hold+n_break`，超时数量另存但不作为这个特征的已判定次数；训练 P-REACT 时仍保留全部成熟超时事件。

结构排序采用 `log1p(n_resolved) + 0.8*stale` 作为最小候选，`stale=1-2^(-age_since_last_contact/120)`；窗口内从未接触时取 1 并标记 `never_contacted=true`。这是参考上游分离位置/排序的设计，是否有效由消融决定；没有已判定历史事件的区域仍可展示，但不能假装有统计优势。[R12]

#### 6.3 区域对象与冻结

```text
Level = {
  level_id, detector_version, data_snapshot_id, created_at, known_at,
  basis_session, basis_price_scale, low, high, center, atr_at_creation,
  kind, location_score, sr_structure_score, stall_score,
  confirmed_pivot_count, historical_resolved_count, last_contact_at,
  provenance, quality_flags
}
```

入场形态开始时冻结区域边界和版本，不能随着后续行情重新画区间后再宣布原突破成功。只有显式公司行为映射可改变价格尺度并留痕。新识别区域可以创建新 `level_id`，不得覆盖旧计划的区域。

### 7. 首个完整交易策略 S-BR：突破回测做多

#### 7.1 冻结的定义

在会话 `b-1` 结束时，从当时已知阻力区域中选择最近的合格区域 Z，要求区域在该时点确实为阻力，且至少有两个已确认枢轴证据。若多区满足，先距离、再位置分数、最后 `level_id` 稳定排序。冻结 `z_low,z_high,A0=ATR_(b-1)`。

`b` 会话出现：`C_b > z_high+0.2*A0`、`C_b>O_b`、`CLV_b>=0.65`、`body_ratio_b>=0.50`，且 `TR_b<=2.5*A0`。使用 b 收盘时已知信息判断；不得在 b 开盘声称已知突破。极长高潮棒不追。

#### 7.2 状态机

```text
IDLE → BREAKOUT_SEEN → HELD_ABOVE → RETEST_SEEN → CONFIRMED
                                                    ↓
                                     PLANNED → FILLED / EXPIRED
任一未成交状态 → INVALIDATED / EXPIRED → COOLDOWN → IDLE
```

| 转换 | 明确规则 |
|---|---|
| IDLE→BREAKOUT_SEEN | 满足突破棒条件；保存 b、Z、A0 |
| BREAKOUT_SEEN→HELD_ABOVE | 最早 b+1；至少一根后续收盘仍高于 `z_high` |
| HELD_ABOVE→RETEST_SEEN | 最早 b+2、最晚 b+10；某棒低点进入 `z_high±0.35*A0`，且收盘不低于 `z_low` |
| RETEST_SEEN→CONFIRMED | 回测棒自身或之后最多 2 会话内：阳线、CLV≥0.65，收盘>`z_high+0.1*A0`且收盘>前一棒高点 |
| CONFIRMED→PLANNED | 当时大盘/个股、目标空间、资金、风险全部通过 |
| 任意未成交→INVALIDATED | 任意收盘低于 `z_low-0.5*A0`、进入 INVALID/CHAOS/DOWNTREND、源区域不可再映射 |
| 任意未成交→EXPIRED | 总形态超过 b+12，或回测后两会话仍无确认，或计划未在下一会话执行窗口内成交 |
| 终态→COOLDOWN | 同股票同区域至少 5 会话不再产生同类新计划 |

状态每根棒最多前进一个阶段；唯一例外是 `RETEST_SEEN` 当棒可同时满足确认条件，因为两者均在该棒收盘才知道，实际执行仍在下一会话。不得在首次突破棒同时“突破、回测、买入”。每日更新状态前先检查已有计划的取消条件。

#### 7.3 信号、价格与失效点

设确认日为 s，信号参考入场 `E_ref=C_s`。

```text
S0 = min(z_low, 从首次回测到s的最低价) - 0.20 * ATR_s
R_ref = E_ref - S0
允许 0.75*ATR_s <= R_ref <= 3.0*ATR_s
```

若有高于参考入场的合格阻力，最近阻力下沿减 `0.1*ATR_s` 为目标候选 T；不能越过最近明显阻力，只取更远目标来凑盈亏比。若没有更高的合格阻力，取 `T=E_ref+2*R_ref`，明确标为**风险倍数退出规则**，不是预测股价或虚构阻力。显示用的 MM 参考不替代该目标。

所有价格按证券 tick 对齐：买入区间下界向上取整、上限向下取整，保护阈值向下取整，目标向下取整。必须满足 `S0<E_ref<T`。以保守每股双边成本 `c` 计算：

```text
RR_net = (T - E_ref - c) / (E_ref - S0 + c)
要求 RR_net >= 1.5
```

最低佣金使 c 与数量相关时，先按参考数量求解，再对最终整手数量重新核验，不能按大单每股佣金应用到小单。

#### 7.4 下一会话入场，不提前使用开盘信息

s 收盘生成**待确认计划**，有效期仅下一交易会话的配置执行窗口。入场允许区间为：

```text
lower = max(S0 + price_tick, E_ref - 0.25*ATR_s)
upper = E_ref + 0.50*ATR_s
```

次一会话实际可观察到执行报价时，重新检查价格区间、停牌/限制、真实可用现金、计划取消标志和 RR。价格超界就取消，不追、不把上限向上挪。计划数量在 s 已预留预算，执行时只能减少，不允许因低开自动加大数量。

日线参考回测将 `E=next_open+买入滑点` 作为执行估计，并同样受区间和风险检查。它是回放模型，不代表用户一定能在该价成交；仅有日线且开盘已触及买入限制价、没有可信排队/成交深度信息时，保守记为不成交并标记原因；不得读取当日最终收盘/总成交量来反过来决定开盘时下不下单。真实信号账本、参考仿真账本和实际成交账本必须分开。

在缺少盘中数据的本地人工模式，用户应在规定窗口核对价格并确认实际成交；系统未收到成交时不得自动创建真实仓位。交易计划过期不等于持仓已平仓。

#### 7.5 退出：默认收盘判断、下一会话执行

入场绑定 `S0,T,R0=E-S0`，风险服务每日检查，按以下优先级生成退出计划：

| 优先级 | 原因 | 触发 |
|---|---|---|
| 1 | 人工紧急退出/账户强制约束 | 形成需确认操作，不伪造执行 |
| 2 | 已生效止损阈值 | `C_t <= S_active` |
| 3 | 结构失效 | 连续两次收盘低于 EMA60，或已有结构跌破条件触发 |
| 4 | 目标到达 | `C_t >= T` |
| 5 | 进展不足 | 已持有至少 5 会话，最高收盘浮盈未达 0.5R0，且 `C_t<EMA20_t` |
| 6 | 时间退出 | 从成交会话计数达到 20 会话 |

所有触发在 t 收盘后得到，最早下一可卖会话执行。EOD 模式下，仅盘中最高价碰目标或最低价穿阈值，**不记作已止盈/止损成交**；可以单独显示风险轨迹。上市板块的可卖数量、停牌、跌停排队等仍可能阻止退出。

跟踪阈值默认在最高收盘浮盈达到 1R0 后激活：`S_next=max(S_active, highest_close_since_entry-2.5*ATR_t)`。先用旧阈值判断本日退出，再计算新阈值，自下一会话生效；只能收紧，不能放宽。未达到激活条件时保持 S0。不采用未经验证的分批减仓，以免首版引入额外路径与归因歧义。

#### 7.6 观察信号与操作信号的区别

`BREAKOUT_SEEN` 通知可以写“进入观察，等待回测”，不得写“立即买入”。只有 `PLANNED` 且通过组合预算、尚未过期的对象才可标为“可核对的操作计划”。未达到统计模型条件的规则策略仍可回测，但不得显示伪造的“预计胜率”。

### 8. 第二个策略 S-H2：上行回撤的二次确认

该模块是对 PA 双腿计数思想的工程化定义，不声称完整复刻所有主观 Brooks 形态；默认关闭，独立完成样本与状态测试后启用。[R06][R09]

状态顺序固定：`UP_LEG → PULLBACK_1 → ATTEMPT_1 → PULLBACK_2 → ATTEMPT_2_CONFIRMED`。

在 `UPTREND` 中保存最近已确认摆动高点与前一确认低点。第一回撤需要至少两个不同会话，且从该高点回落不少于 0.5 ATR（使用回撤开始时冻结 ATR）；回撤期间低点接近 EMA20 或已冻结支撑区域，距离不超过 0.5 ATR。首次收盘突破前一棒高点记 ATTEMPT_1，但首版不交易 H1。

ATTEMPT_1 后必须出现**新的**至少两会话回撤，其中至少一会话收盘低于前收盘，才进入 PULLBACK_2；连续高点上移不构成第二回撤。第二回撤累计幅度仍须不低于 0.5 个冻结 ATR，且不能收盘跌破前一确认低点减 0.2 ATR。第二次出现阳线、CLV≥0.65、收盘超过前一棒高点时，才产生 H2 确认。

每一腿保存开始、结束、极点和确认时点。第一回撤开始后最多 20 会话；出现新确认高点且超过原高点、趋势转空、混乱或结构失效时重置。每棒最多推进一个状态。两条回撤腿不能共享同一根棒。

止损为两腿最低价与绑定支撑下沿的较低者减 0.2 ATR；没有绑定支撑时使用两腿低点。目标、入场有效期、RR、持仓退出、风险预算与 S-BR 相同。不得借 H2 标签自动赋予比其他形态更高的获利概率。

### 9. 第三个策略 S-RG：偏多区间下边缘反弹

该模块只处理 `BIASED_RANGE`，不是所有横盘股票的抄底器，且默认关闭。中性区间不交易，保持与 PA 方向约束的基本思想一致。[R07]

上下区域在首次接近下沿之前冻结，各至少两次已确认枢轴证据，两个区域中心距离位于 3–8 ATR。入场位置必须处于整个区间下三分之一且距离下方支撑区域不超过 0.35 ATR。出现阳线、CLV≥0.65、收盘重新高于支撑区域上沿、收盘突破前一棒高点后，下一会话按共同执行规则核验。

初始阈值设在支撑下沿与确认棒低点较低者下方 0.2 ATR；目标为上方阻力下沿减 0.1 ATR。没有明确对侧区域则不交易，不用 2R 人造目标弥补无边界的区间。RR 必须达标。最大持有期 10 会话，其他退出规则沿用公共策略契约。

若区间中部、下行趋势、扩张失控或 CHAOS，则不产生新多头；区间下沿被跌破不是“便宜了再加仓”。

### 10. 独立基线与挑战者

这些对象必须能单独回测和比较，不能只在代码里作为几个布尔指标。

| ID | 完整但简化的交易定义 | 用途 |
|---|---|---|
| `B-CASH` | 相同现金账户，不开仓；按配置计算现金利息，没有配置则 0 | 验证策略是否优于不交易 |
| `B-HOLD` | 匹配市场可投资基准与明确建仓规则，计分红、费用 | 检验相对收益与风险暴露 |
| `B-EMA` | 收盘 EMA20 上穿 EMA60 后下一会话买；下穿后下一会话卖；无额外形态 | 检验复杂化是否有收益贡献 |
| `B-DC` | `C_t>max(H_(t-20..t-1))` 后下一会话买；`C_t<min(L_(t-10..t-1))` 或固定 2ATR 风险阈值收盘失守后退出 | 参考 Turtle 的长多无加仓改写 [R19] |
| `B-MOM` | 月末对 `TR_(t-21)/TR_(t-252)-1` 排名，取前 K；等权目标，下一会话调仓，限制每期更换数量 | 参考滞后动量与 TopkDropout 的长多改写 [R21][R24] |
| `C-BOLL` | 上一会话确定的 Boll(20,2) 上轨被收盘突破且 CCI20>0，下一会话买；收盘跌破 EMA20 或 ATR 跟踪阈值后退出 | 参考 Boll/CCI 的日线挑战者，非原参数复现 [R20] |
| `C-ML` | 用经验证模型得分排序，按相同组合约束和换手规则形成目标 | 研究独立预测能力，不作为“AI 必胜”模块 [R22–R24] |

没有结构止损定义的基线，以 2ATR 压力距离做仓位预算，并明确这只是预算基准，不伪装成该基线自带止损。额外加共同保护退出时，登记为独立 `+risk_overlay` 版本，不能悄悄修改原基线。

所有技术策略采用相同市场约束、成本与下一会话口径。基准比较至少提供两套：各策略本来定义的资金暴露，以及统一敞口/风险预算后的比较。不能用主策略只有 20% 仓位的收益率直接与满仓基准得出“无效/有效”，却不显示敞口、波动与回撤。

B-DC 中窗口必须排除当前棒，否则 `C_t > max(含H_t的窗口)` 在正常 OHLC 下无法成立。多周期窗口的 `shift`、价格单位和标签时点必须有专门测试。

### 11. 组合风险与仓位数量

#### 11.1 默认参数，全部属于待确认的风险配置

| 参数 | 起点 | 说明 |
|---|---:|---|
| 每笔计划风险预算 | 净资产的 0.25% | 含预设跳空缓冲与预估成本，不是最大实际亏损保证 |
| 单股票市值上限 | 净资产的 10% | 包含该股票已有手工仓位与未成交计划 |
| 单行业敞口上限 | 净资产的 25% | 行业未知按独立受限桶，不当作“分散” |
| 总多头市值上限 | 净资产的 80% | 无融资、无负现金 |
| 持仓股票数上限 | 8 | 按账户唯一证券数，不按策略实例数 |
| 组合计划风险上限 | 净资产的 2% | 现仓+新计划；不因浮盈就把隔夜风险记为 0 |
| 单次交易容量上限 | ADV20 股数的 0.1% | 容量研究起点；日线 ADV 不能保证开盘容量 |
| 隔夜缓冲 | 每股 0.5 ATR | 用于预算；实际跳空可能超过缓冲 |
| 策略级暂停新增 | 高水位回撤超过用户设定阈值，模板为 10% | 不自动销毁仓位或忽略退出；人工复核后恢复 |

持有手工股票但没有可信保护阈值时，以 `max(2*ATR, 市值对应的配置压力跌幅)` 估算压力风险；缺 ATR/估值则标为未知风险并暂停新增，不能默认零风险。行业等约束必须以最近可用且有时间标记的信息计算。

#### 11.2 数量计算

设权益 V、入场 E、止损阈值 S、每股成本 c、跳空缓冲 g。基础每股预算风险：

```text
r_share = max(E-S, price_tick) + g + c
q_risk = floor(V * risk_fraction / r_share)
q_cap = floor(剩余单股市值预算 / E)
q_cash = 最大满足 q*E + entry_fee(q) <= 未冻结可用现金 的整数q
q_liquidity = floor(ADV20_shares * 0.001)
q_pre = min(q_risk, q_cap, q_cash, q_liquidity, 行业/总敞口余量允许的数量)
q = market_rules.normalize_buy_quantity(q_pre)
```

不能全市场固定“100 股一手”。最小数量、递增单位、零股卖出、价格跳动和可卖时间均来自证券适用规则。数量规范化后必须重新计算最低佣金、RR、现金、总风险，直到满足或 q=0；q=0 就跳过，禁止凑一手超过风险。

现仓预算风险以当前估值到已生效保护阈值的距离加跳空/费用缓冲估算，而不是始终使用原始买入价；跟踪阈值高于买入价时仍保留隔夜风险。

入场前为计划冻结风险与现金额度，多个策略并发不能重复使用同一额度。真实成交部分执行时只管理实际数量，剩余部分取消或继续执行必须明确，首版默认取消未成交余量并解冻预算。

#### 11.3 风险优先关系

`数据/账户真实性 > 市场交易限制 > 组合硬风险 > 已持仓退出 > 新策略信号 > 排名/解释`。

预测高概率不能绕过风险；用户临时修改参数必须生成审计事件，不能改写过去结果。对受 T+1 或其他可卖限制影响的头寸，触发风险阈值也不能按不可实现的同日卖出记收益。市场参数应按规则生效日建模，而不是写死“所有证券都一样”。[R34]

### 12. 预测模块：先定义问题，再训练模型

#### 12.1 上游双概率模型的继承边界

借鉴上游把“接触区域”和“接触后反应”拆开的方法，但不直接加载其系数或照搬排除未决的训练集。新检测器、新频率、新区域宽度或新标签，都必须形成新的任务版本。[R13][R17]

| 任务 | 预测时点与特征 | 目标及用途 |
|---|---|---|
| `P-TOUCH-v1` | 区域冻结时的 X_t | 未来 40 会话内，从正确方向实际接触区域的概率；用于观察雷达 |
| `P-REACT-v1` | 同一个冻结时点的 X_t，条件为之后确有接触 | 接触后明确 `hold/break/timeout` 的条件分布；用于区域研究 |
| `P-TRADE-v1` | 交易确认时点 X_s，含执行口径与形态 | 按同一策略模拟执行后的净收益分布；用于交易过滤 |
| `P-RETURN-h` | s 收盘 X_s | 从 s+1 开盘到 s+1+h 开盘的总回报分布，h∈{1,5,20}；不混用即时涨跌 |

`P-RETURN` 的一会话收益不是“今天买入今天卖出”的收益，且回测需考虑最早可卖时点、费用和股票终止交易处理。没有合适公司行为/终值数据的样本不能悄悄删除而不报告。

#### 12.2 P-TOUCH 和 P-REACT 的标签精确定义

冻结时价格必须在区域正确一侧。支撑的正确接触是此前价格在区域上方，后续某根完整高低区间与区域有交集；阻力反向。若价格直接跳过整个区域、该棒亦无交集，则记 `gap_invalidation`，与真实接触分开，不伪造区间成交。

P-TOUCH 的事件是 40 会话内、且在 gap_invalidation 之前发生接触。接触前 gap_invalidation 是竞争终态，计入非接触类别并单列数量。没有接触且窗口完整结束为 no_contact；数据截断、无法观察后续或不可解释终止为 censored。首版逻辑回归对完整结果做接触/非接触分类，同时报告竞争终态占比。

对接触日 j：若支撑在 j 收盘已经低于 `z_low-0.5*A0`，立即记 break；否则在 j+1 至 j+10 的收盘序列中，首先收于 `z_high+1.0*A0` 上方为 hold，首先收于 `z_low-0.5*A0` 下方为 break。阻力按方向对称。完整观察 10 会话均未发生为 timeout。采用收盘判定，避免同一 OHLC 棒无法确定先后；j 收盘后的未来 10 会话不包含 j 本身。

首版为统一样本成熟政策，只有 `j+10` 已经过且数据有效才纳入反应训练，即使早期已判定也等待该窗口成熟。**未成熟是 censored，不是 timeout。** 当行情缺数不能判断路径时，保留质量标记，不制造标签。

该定义与上游不同，必须重新训练。它仍然是区域反应任务，不能自动当作 S-BR 成交后的交易盈利任务。S-BR 已经历回测后，原区域接触是已发生事实，此时不拿预测 P-TOUCH 当新增买入门槛。

#### 12.3 P-TRADE 的标签与执行选择

记录所有合格候选，包括被风控拒绝、价格超界、未成交、人工忽略的候选。模型训练分开处理“可成交性”和“成交后收益”，不能把未成交行当零收益盈利样本，也不能只选择人工愿意交易的赢家。

成交后标签来自同一执行模拟器，包含退出原因、净收益、以初始 R0 归一的净 R 倍数、最大不利/有利变动、持仓期。盈利概率为 `P(net_pnl>0 | 可执行入场, X_s)`；收益期望用全部成交结果估计，包括时间退出和其他中间收益，不套用只有固定 TP/SL 两结局的公式。

实时入场价格相比训练参考的漂移若超过允许区间，模型失配，直接取消或用匹配执行模型重新评估；不能只替换价格而保持原获利概率。

#### 12.4 训练顺序与校准

先建立基准发生率、正则逻辑回归/线性回归；确认数据和标签正确后，才引入 LightGBM 与挑选后的 Alpha158 特征。标准化、缺失值处理、特征选择及概率校准全部只拟合对应训练/验证段。Qlib 提供的是工作流和特征参考，不提供对本任务的效果担保。[R22][R23]

校准采用单独验证段上的 sigmoid 或足够数据下的 isotonic；必须展示可靠性分箱、样本数量、Brier/log-loss 及发生率基线。AUC 表示排序能力，不等于概率准确；校准也不自动创造盈利能力。预测区间应报告实际覆盖率，而不是把一个模型分数重新缩放成“可信度”。[R32]

#### 12.5 统计过滤模式

`rule_only` 与 `statistical_filter` 是不同的已冻结策略版本。后者要求匹配 `detector_version, feature_schema_hash, label_version, execution_profile, market, horizon` 的模型，才允许使用 P-TRADE 的净收益期望进行过滤。

初始过滤规则可设为：验证段确定的预期净 R 门槛不低于 0；在最终样本外验证其过滤后收益及覆盖率。没有通过验证或模型过期时 `ABSTAIN_MODEL_UNAVAILABLE`，不得静默切换成未批准的规则版本。合法的规则版可以作为独立已批准实例继续运行，但应产生可见的部署切换记录。

不输出通用“胜率至少 60% 才买”。低胜率高赔率、时间退出、成本和不成交都必须进入真实收益分布。

### 13. 回测、验证与上线条件

#### 13.1 同一引擎三种运行

`HistoricalReplay`、`PaperRunner`、`LiveAssistRunner` 必须调用同一策略状态函数和同一风险服务。不同之处仅是事件来源、时钟和成交适配器。历史模式禁用网络与系统当前时间，事件可重复排序；随机数使用固定种子和稳定哈希，不能用进程随机化的内建字符串 hash 生成跨进程实验种子。

回测必须包括现金、佣金最低值、卖出税费、股数/价位规则、复权映射、分红、停牌、终止交易、无法成交、拒绝计划、部分成交和资金冻结。未知终止回收价值不能直接沿用最后价格无限持有，应显示未决估值和压力情景。

#### 13.2 时间外切分

起点采用滚动 `训练36个月 → 验证6个月 → 测试3个月`，测试段互不重叠。最终另留从未用于调参的保留段。时间长度不足时输出证据不足，不能随机拆股票×日期样本凑齐。

**按交易日期组切分面板数据**，同一天不同股票不散落训练与验证。再按每条样本的 `[feature_asof,label_end_at]` 区间清除与验证/测试标签窗口重叠的训练样本。只在一维日期上设 gap 不足以自动解决所有多标的重叠标签；需要本项目区间 purge。[R33]

区域任务可能需要 40 会话等接触、再 10 会话等反应；不能简单只隔 5 或 20 根。实际 purge 以各样本 label_end_at 为准。跨股票留出另做稳健性评测，不取代时间切分。

#### 13.3 必须报告的成绩

| 类别 | 必须包含 |
|---|---|
| 数据 | 点时覆盖率、丢失/停牌/删去原因、退市样本、价格/公司行为版本 |
| 交易 | 信号数、可执行数、实际/仿真成交数、拒绝/过期数、成本、换手、持仓期 |
| 收益风险 | 成本后总收益/年化、波动、最大回撤、Calmar、按同口径计算的 Sharpe、净 R 分布、敞口 |
| 对比 | 现金、可投资基准、EMA、Donchian、无 SR、无 RS、无市场闸门等消融 |
| 稳健性 | 分年度、市场状态、行业、流动性与股票贡献；成本翻倍、额外跳空和执行延迟情景 |
| 概率 | Brier、log-loss、校准图、样本数/基线、覆盖率；按完整事件定义分组 |
| 可信度 | 按时间块重采样的区间；实验尝试数；最终保留段是否被查看过 |

高相关、同日出现的多只股票信号不按完全独立样本计算显著性。基础重采样使用连续交易日期块，将同一日期所有股票一起抽取，块长至少覆盖目标/持仓依赖尺度，并提供块长敏感性分析；独立逐交易 bootstrap 只能作为对照。

#### 13.4 参数研究纪律

第一次研究预注册不超过 24 个组合，只搜索 2–3 个最重要参数，例如回测容忍度、最小 RR、时间退出；不一次搜索几十个指标权重。所有尝试包括失败、超时、无交易都入库。用验证段选定一个参数版本，再一次性评测最终保留段。看到结果后修改就属于新研究，原保留段不再是新模型的“未看过测试集”。

默认执行消融顺序：简单突破基线→加回测确认→加 SR 区域→加相对强弱排序→加市场闸门→加统计过滤。若某层增加复杂度却未改善样本外风险收益/稳定性，就移出默认组合。

#### 13.5 晋级规则

工程门槛是硬约束：所有关键测试通过，来源/版本完整，无已知未来依赖，执行模型可解释，风险不能被绕过。

研究门槛默认要求至少 4 个互不重叠时间外测试段、100 笔完整成交结果，并展示集中度与不确定性；这些是证据量起点，不是盈利证明。净期望的时间块重采样区间仍跨 0 时，状态是 `INCONCLUSIVE`，不能包装为已证实优势。策略也应符合用户设定回撤容忍度；回撤容忍度未填写不得批准真实资金辅助。

进入 `LIVE_ASSIST` 前，至少有 60 个交易会话的影子观察及足够的实际可执行计划对照，模板设为 20 次；样本不足继续观察，不扩大仓位凑样本。人工确认、成交回传、休眠恢复、通知延迟与拒绝执行均属于验收内容。上述为系统政策，不是对未来收益的承诺。

### 14. 一个可核对的计划算例

以下是合成算例，不指向真实股票。

```text
账户权益：100,000 元；每笔风险预算：250 元
信号确认收盘参考：10.00 元；ATR：0.50 元
下一会话参考成交（含滑点）：10.05 元
冻结保护阈值：9.40 元；冻结目标：11.40 元
示例双边每股成本：0.03 元（实际系统由数量和费率函数计算）
隔夜缓冲：0.25 元；示例买入单位：100 股
```

实际价差风险 `10.05-9.40=0.65` 元；每股预算风险 `0.65+0.25+0.03=0.93` 元。风险允许数量为 `floor(250/0.93)=268` 股，按示例单位向下规范化为 200 股；仍需检查现金、行业、总敞口和流动性约束。

净结构 RR 为 `(11.40-10.05-0.03)/(10.05-9.40+0.03)≈1.94`，超过默认 1.5。名义投入为 2,010 元、预算风险为 186 元。但日线退出可能在更低的次日价格成交，真实损失仍可能超过 186 元；它不是风险上限保证。

若行情跳到入场上限之外、用户错过执行窗口、或最新权益不足，计划取消。不能为了“有一笔交易”放宽入场价格、移动止损或增加股数。

### 15. 信号输出契约

```json
{
  "schema_version": "1.0",
  "strategy_id": "xq.srpa.breakout_retest.long",
  "strategy_version": "0.1.0",
  "status": "PLANNED",
  "instrument_id": "DEMO.EXAMPLE",
  "simulation_only": true,
  "decision_at": "2026-09-08T08:00:00Z",
  "execution_session_id": "DEMO-NEXT-SESSION",
  "execution_profile": "EOD_ASSIST",
  "entry_reference": "10.00",
  "entry_min": "9.88",
  "entry_max": "10.25",
  "stop_threshold": "9.40",
  "target_threshold": "11.40",
  "quantity_cap": 200,
  "example_price_tick": "0.01",
  "reason_codes": ["BREAKOUT_RETEST_CONFIRMED", "RISK_BUDGET_OK"],
  "probability": null,
  "model_status": "RULE_ONLY",
  "invalidation": ["ENTRY_WINDOW_EXPIRED", "PRICE_OUTSIDE_BAND", "DATA_INVALID"],
  "data_snapshot_id": "demo-snapshot",
  "level_id": "demo-frozen-level",
  "config_hash": "DEMO-NOT-A-REAL-HASH",
  "run_id": "demo-run"
}
```

示例按 0.01 元 tick 对齐，入场下界 9.875 向上规范化为 9.88；真实输出必须经过适用市场的 tick 规范化，不能把占位字符串当作真实哈希、证券、交易会话或已通过验证的价格。真实消息正文必须展示数据时间、执行窗口、取消条件与“收盘阈值≠保证成交价”。

### 16. 首版完成的定义

首版不是“有一个策略页面”。它必须能在本地从固定行情快照出发，运行 S-BR 和至少两个基线，生成逐棒状态轨迹、真实约束下的参考成交、费用/权益曲线、拒绝原因、与同一输入下完全一致的重跑结果。之后接上策略版本管理、仿真、通知与真实成交复盘。

当数据或证据不足时，系统必须能明确输出“暂无达到部署条件的策略”，同时保留全部研究工作。这比把未验证的策略包装成现成赚钱系统更接近实际可用的量化工程。

---

## 第三篇：本地量化策略管理系统设计

*原分篇文件：`03_STRATEGY_PLATFORM_DESIGN.md`。*


> 版本：2.0；日期：2026-09-08。  
> 产品定位：**本地策略研究、验证、部署与复盘工作台**。股票页面是策略的观察界面，不再是产品主线。  
> 首批内核：SR 关键位检测、PA 确认状态机、策略/账户风控、事件回放、策略版本与实验管理。  
> 详细规则以 `02_SRPA_STRATEGY_SPEC.md` 为准，来源证据以 `01_SOURCE_AUDIT.md` 为准。

### 1. 与上一版设计的关系

本版本替代旧 `X-Stock_Design.md` 中的**产品优先级、默认部署方式、策略研究顺序和 Codex 阶段计划**。旧文档中数据授权、重试限流、账本完整性、不可伪造预测、用户确认等原则仍有效，但发生冲突时以本交付包为准。

| 上一版重点 | v2 调整 |
|---|---|
| 持仓/自选网站先完成，策略与验证后续增加 | 先让策略在本地正确回放，再实现管理界面 |
| 策略接近几个分析指标 | 策略是有版本、状态、入场、退出、风险、执行和验证记录的完整资产 |
| 参考两个仓库的工作流 | 逐条提取策略与预测逻辑，区分代码、主观规则、统计模型和未经验证假设 |
| 默认服务端部署、多个基础服务 | 本地进程优先，轻量数据库与文件快照，数据库服务/队列按需扩展 |
| 股票分析报告是主要结果 | 核心结果是可追溯策略计划、样本外证据、真实执行差异与迭代决策 |

#### 1.1 核心使用闭环

```text
发现/登记策略 → 审查与固定版本 → 规格化 → 合成测试 → 回放回测
            → 样本外与消融 → 仿真/影子 → 人工批准辅助运行
            → 实际成交/忽略/拒绝 → 归因复盘 → 新版本研究
```

不得自动跳过“验证”从互联网代码直接进入持仓提醒。不得用“最新收益最高”自动替换正式版本。自动化的是研究任务和证据收集，资金相关部署仍需要明确批准。

### 2. 本地优先架构

#### 2.1 默认技术选型

| 层 | 方案 | 设计理由 |
|---|---|---|
| 策略计算 | Python 纯领域包，NumPy/Pandas 或 Polars，明确浮点/Decimal 边界 | 与网页和数据源独立，可直接测试与批处理 |
| API/协调器 | FastAPI + 单个 LocalCoordinator | 同一业务命令用于网页/CLI；统一调度和账本写入 |
| 网页 | React + TypeScript + Vite 构建 SPA，构建产物由 API 提供 | 不需要默认 SSR 服务；本地同源访问 |
| 元数据与账本 | SQLite WAL、迁移脚本、事务与备份 | 个人本地负载起点；协调器串行写入，避免到处写库 |
| 行情/特征 | Parquet 不可变分区 + DuckDB 只读分析 | 大批量历史查询不占用业务事务表 |
| 作业 | SQLite 持久任务 + 受控子进程 + IPC 结果返回 | 首版不强制 Redis/Celery；崩溃后可恢复 |
| 工件 | 本地内容寻址目录：哈希、版本清单、只读数据快照 | 数据/模型/报告可追溯，不覆盖上次实验 |
| 图表 | 浏览器 K 线与曲线组件，可替换 | 图表只能展示事实与记录，不能承载策略逻辑 |
| 可选研究工具 | Qlib/MLflow 适配器 | 参考其训练、工件与版本思想，不强制整个平台依赖 [R22–R24][R27] |

初期不默认 Kubernetes、Kafka、微服务、分布式特征库或多个回测框架并存。需要远程多用户或吞吐扩大时，再通过既有 Repository/JobQueue 接口替换为 PostgreSQL 与外部任务服务。

#### 2.2 进程和数据流

```text
本地浏览器 ──同源 HTTP──┐
本地 CLI ──────────────┼─> LocalCoordinator / API（唯一业务写入协调点）
                      │        ├─ Strategy Registry / Risk / Ledger
                      │        ├─ SQLite：版本、作业、事件、账本、审批、Outbox
                      │        ├─ Scheduler：市场日历、预算、恢复策略
                      │        └─ 子进程任务池（输入快照→纯计算→结果）
                      │                    └─ 只读 Parquet / 特征 / 模型
                      └─> 页面只读查询缓存

授权 Provider → DataHub → 暂存文件 → 校验 → 原子发布快照
业务事件 → Outbox → 通知适配器 → 投递状态（不等于成交状态）
真实成交导入 → 幂等校验/对账 → 账户账本 → 策略归因
```

策略计算子进程不直接写账户账本或业务数据库，不读取消息密钥。Coordinator 接收结构化结果，在事务中保存事件、状态检查点与 outbox。长回测不得占据实时持仓监控队列。

Parquet 发布采用临时路径写完、校验、生成 manifest、原子替换 manifest 指针；历史 manifest 指向的文件不被原地改写。DuckDB 分析连接只读取指定快照，不在并发子进程中修改同一个数据库文件。文件目录与 SQLite 元数据发布需采用可恢复的两阶段业务流程，残留暂存文件可回收。

#### 2.3 本地运行模式

`research`：只读行情与模拟账户，不外发、不碰真实账本。  
`paper`：按真实时钟跑计划，但成交进入仿真账本；可发明确标识的测试消息。  
`live_assist`：通过批准的实例，输出人工可核对计划；只在实际成交导入后记真实持仓。  
`demo`：合成证券和行情，界面水印，不能发成真实投资提醒。

上述模式的数据库命名空间、工件目录和通知策略独立。旧实验无法因切换一个全局开关变成真实交易实例。

### 3. 策略管理领域模型

#### 3.1 不能混为一谈的八个对象

| 对象 | 例子 | 关键属性 |
|---|---|---|
| Source | 一个 GitHub 仓库/一份研究资料 | URL、SHA、文件路径、许可、审查结论 |
| Component | 区域检测器、H2 状态机、ATR 风险政策 | 输入输出 schema、版本、测试、依赖 |
| StrategyDefinition | 突破回测做多这一策略族 | 假设、适用市场、参数 schema、所有者、标签 |
| StrategyVersion | `0.1.0 + config_hash` | 不可变代码/参数/标签/执行模型、依赖版本 |
| Experiment | 某次数据快照上的比较回测 | 输入、尝试参数、结果、状态、证据等级 |
| ModelArtifact | 一个概率/收益模型文件 | 训练截止、特征、标签、校准、适用范围、文件哈希 |
| StrategyInstance | 将某版本绑定账户/股票池运行 | 模式、预算、时间表、部署状态、版本钉住 |
| Decision/TradePlan | 某日某股的计划 | 时点、依据、风险预留、有效期、实际执行反馈 |

参数改动产生新 StrategyVersion，而不是更新同一个历史对象。实例可以切版本，但已存在的计划和持仓不自动重绑。模型文件换了，即使策略源代码没变，也构成新的部署版本。

#### 3.2 策略包规范

每个策略包必须有 `manifest.yaml, parameters.schema.json, strategy.py, tests/, strategy_card.md`。复杂策略增加 `labels.py`、`features.py`、`state.schema.json`。manifest 至少声明：

```text
strategy_id, version, engine_api_version, source_refs, license_review
entrypoint, side, supported_markets, frequency, warmup_sessions
feature_schema_version, state_schema_version, required_capabilities
default_execution_profile, parameters, risk_policy_ref
lookahead_tests, state_transition_tests, benchmark_refs
```

manifest 和参数 schema 仅描述受支持的条件，不允许输入任意字符串表达式后 `eval`。参数严格类型、范围、互斥和依赖校验；数值优化范围必须显式设定，不接受无限制自动搜索。

#### 3.3 状态与审批

版本的研究生命周期：

```text
DRAFT → SPECIFIED → UNIT_VERIFIED → BACKTESTED → OOS_PASSED
                                                ↓
                                   PAPER_VERIFIED → APPROVED_LIVE_ASSIST

任意阶段可标记 INCONCLUSIVE / REJECTED / ARCHIVED；不能擦除旧证据。
```

实例的运行状态：`STOPPED / RUNNING / PAUSED_ENTRIES / EXIT_ONLY / ERROR / DISABLED`。

| 操作 | 精确定义 |
|---|---|
| 暂停新增 | 不再生成新的买入计划；已有持仓风控继续 |
| 只出不进 | 取消未成交新增计划，继续生成已持仓退出提醒 |
| 停止实例 | 停止策略主动计算，但把持仓移交独立 RiskGuardian 监控，并提示退出逻辑降级 |
| 禁用版本 | 禁止新部署与新开仓；旧持仓必须保留可读的原始风险规则 |
| 回滚 | 未来新信号改用已批准旧版本；不会删除已成交交易或重写过去绩效 |
| 退役 | 无未结计划/受管持仓，或完成显式接管后归档 |
| 一键退出 | 生成待确认退出操作，不直接把仓位清零；无券商连接不得写“已平仓” |

个人单用户可以自己审批，但仍需记录是谁、何时、基于哪些报告、哪种执行口径和风险限额批准。批准时版本哈希与报告对应关系必须校验。

### 4. 策略管理功能清单

#### 4.1 第一优先级：策略资产、参数、实验

| 功能 | 具体行为 | 实施阶段 |
|---|---|---|
| 来源登记 | GitHub/论文/资料链接、路径、SHA、许可和审查说明 | P0 |
| 策略卡片 | 交易假设、市场/周期、入场/退出、风险、证据等级 | P1 |
| 策略创建/复制 | 从模板建立新策略或克隆为研究分支 | P1 |
| 参数管理 | schema 表单、范围验证、导入/导出 YAML、参数差异 | P1 CLI，P3 网页 |
| 版本管理 | 不可变版本、代码/参数/特征/模型 diff、来源升级提醒 | P1–P3 |
| 依赖检查 | 缺字段/窗口不足/交易规则未知时拒绝启动 | P1 |
| 单策略回放 | 逐棒查看状态、信号与权益变化 | P1–P2 |
| 批量回测 | 明确日期/股票池/参数网格与任务预算 | P2 |
| 基线比较 | 相同数据与执行成本下并排比较 | P2 |
| 消融 | 删除 SR/RS/状态闸门/模型层形成独立实验 | P2 |
| 防泄漏检查 | 前缀一致、污染未来、标签成熟、枢轴确认 | P1–P2 |
| 样本外管理 | 时间段锁定、purge、实验尝试审计、防测试集复用 | P2 |
| 研究报告 | 成本后曲线、回撤、交易分布、拒绝信号、置信区间 | P2–P3 |
| 暂无交易诊断 | 逐层过滤漏斗，显示哪个条件阻止了多少候选 | P2–P3 |

#### 4.2 第二优先级：部署、组合与复盘

| 功能 | 具体行为 | 实施阶段 |
|---|---|---|
| 股票池绑定 | 全市场/行业/价格/自选/组合持仓；保存每日点时快照 | P2–P3 |
| 实例预算 | 按账户设置最大资金、风险、行业/证券限额 | P2–P3 |
| 仿真部署 | 实时同代码、仿真账本、与历史回放对照 | P3 |
| 启停与回滚 | 明确新增/退出/已有持仓处理，不含糊 | P3 |
| 信号订阅 | 某策略、某组合或某股票；级别和通知时段可配 | P3 |
| 提醒闭环 | 观察/可核对计划/退出/失效，确认、忽略、过期 | P3 |
| 持仓接管 | 既有手工仓位建立独立接管记录与政策 | P3 |
| 执行偏差 | 参考计划与真实成交的价格、数量、时间差 | P3–P4 |
| 逐笔复盘 | 信号当时图表、状态轨迹、数据版本、取消/退出原因 | P3–P4 |
| 组合归因 | 策略、行业、市场状态、成本与执行偏差分别贡献 | P4 |
| 健康监控 | 数据老化、任务延迟、异常换手、回撤、模型漂移 | P3–P4 |
| 模型注册 | 特征/标签/校准版本、训练截止、失效/回滚 | P4 |
| Champion/Challenger | 新版本旁路观察，不夺取旧版本预算 | P4 |
| 导出/迁移 | 策略包、实验报告、JSON/CSV/Parquet 和可恢复备份 | P3–P4 |

#### 4.3 后续增强，不阻塞首个可用策略

可视化规则组合器只能组合已注册的安全节点，保存的结果必须仍可导出、版本化、测试。支持研究沙箱、成本敏感性、策略相关性、风险预算组合与再训练候选，但不允许“每晚自动选择最赚钱参数然后上线”。外部自动交易连接作为单独产品阶段，须重新设计券商订单状态和独立安全开关。

### 5. 领域接口：同一策略不认识网页和数据供应商

以下为待实现契约，不是声称已有可运行库。跨包数据结构必须有类型和 schema 版本。

```python
from dataclasses import dataclass
from datetime import datetime
from typing import Mapping, Protocol, Sequence

@dataclass(frozen=True)
class EvaluationContext:
    event_kind: str
    decision_at: datetime
    snapshot_id: str
    calendar_version: str
    execution_profile: str
    state: Mapping[str, object]
    position_view: Mapping[str, object]
    features: Mapping[str, object]

@dataclass(frozen=True)
class StrategyIntent:
    instrument_id: str
    action: str  # WATCH, PROPOSE_ENTRY, PROPOSE_EXIT, CANCEL_PLAN, ABSTAIN
    reason_codes: tuple[str, ...]
    payload: Mapping[str, object]

@dataclass(frozen=True)
class EvaluationResult:
    next_state: Mapping[str, object]
    intents: Sequence[StrategyIntent]
    trace: Sequence[Mapping[str, object]]

class Strategy(Protocol):
    def evaluate(self, ctx: EvaluationContext) -> EvaluationResult: ...

class RiskPolicy(Protocol):
    def assess(self, intent: StrategyIntent,
               account_snapshot: Mapping[str, object]) -> Mapping[str, object]: ...
```

`Strategy.evaluate` 只能消费传入的时间切片与状态，不能调用系统当前时间、外网、通知或账户写入。输入状态视为不可变；输出下一状态由引擎原子保存。金钱/成交价通过 Decimal/定点类型进入账本，特征计算可使用浮点，序列化精度与比较容差需要固定。

Provider 返回标准化数据而不是直接生成买卖决策。ForecastModel 返回有目标定义的 Forecast；它不能私自下单。NotificationAdapter 只投递已落库消息，不能从推送成功推导“成交”。

#### 5.1 事件链

```text
BarsPublished → FeatureBatchReady → StrategyEvaluated
→ CandidateBatchRanked → RiskReserved → PlanPublished
→ UserAcknowledged / PlanExpired / PlanCancelled
→ ExecutionImported → LedgerUpdated → PositionReviewed
→ ExitPlanPublished → ExecutionImported → AttributionReady
```

对同一候选批次，先完成统一数据截面的全部候选，再按固定排序分配风险；不能让最快返回的线程抢走所有预算。持仓退出比新仓扫描优先，不能等全市场训练完成才处理止损提醒。

### 6. 数据库与工件结构

#### 6.1 最小表集合

| 组 | 表 | 不可省略的约束 |
|---|---|---|
| 来源 | `sources, source_snapshots, source_reviews` | 快照唯一 SHA/哈希；审查记录 append-only |
| 策略 | `strategy_definitions, strategy_versions, component_versions` | `(strategy_id,version)` 唯一，已冻结版本不可 UPDATE 内容 |
| 参数 | `parameter_sets, search_spaces` | 内容哈希、范围、策略兼容性、实验预算 |
| 数据 | `data_snapshots, universe_snapshots, artifact_manifests` | immutable manifest、PIT 标记、能力与质量标记 |
| 研究 | `experiments, experiment_trials, evaluations, model_artifacts` | 所有尝试入库，训练/验证/测试清单与代码哈希 |
| 部署 | `strategy_instances, deployments, approvals` | 实例钉版本；批准必须匹配报告/版本 |
| 运行 | `jobs, checkpoints, event_log` | 作业幂等键、租约/尝试号、顺序号与恢复点 |
| 决策 | `signals, decision_traces, trade_plans, risk_reservations` | 批次唯一、有效期、拒绝原因；冻结额度可对账 |
| 账户 | `accounts, ledger_events, positions, position_lots, execution_imports` | 成交幂等；持仓由账本投影；真实/仿真隔离 |
| 通知 | `subscriptions, notification_outbox, deliveries, user_feedback` | 业务去重键、投递重试、过期/撤销与幂等确认 |
| 监控 | `health_events, drift_reports, audit_log` | 时间和对象可追溯，敏感字段脱敏 |

特征和完整行情存 Parquet，SQLite 存索引/manifest/摘要。策略轨迹体积大时落工件文件，表里保存 hash 与位置。默认不物理删除被任何实验、交易、审批引用的对象。

#### 6.2 实验指纹

必须保存：`code_commit, strategy_version, config_hash, component_versions, feature_schema_hash, dataset_manifest_hash, universe_snapshot_hash, corporate_action_vintage, calendar_version, market_rule_version, cost_profile_hash, execution_profile, model_hash, seed, dependency_lock_hash`。

同一环境下重复回放应有一致的事件与成交轨迹；跨硬件浮点结果按已声明精度容差核对，不能把模型训练的非确定性藏在“策略没变”下面。浮点容差不能放宽成价格跨了一个 tick 仍算一致。

#### 6.3 真实持仓与策略归因

实际账本是权威。持仓投影按成交、手续费、分红、拆并股、现金流等重建。真实成交必须带 `broker_execution_id` 或可审核的导入批次去重键，重复上传不能重复加仓。

同一股票多个策略持有时，真实数量按账户净额管理，归因通过 lot/虚拟策略份额记录，总和必须等于实际数量。首版默认禁止同一股票被多个实例同时新增，以减少归因与互相对冲问题。无策略来源的既有仓位标记 `MANUAL_UNASSIGNED`，不能把其既往涨幅记作新策略收益。

用户确认消息只是 `ACKNOWLEDGED`；只有 `ExecutionImported` 才可转 `FILLED`。忽略、取消、部分成交、成交迟到都必须保存，不能从报表中消失。

### 7. API 与 CLI

#### 7.1 API 分组

| 接口 | 行为 |
|---|---|
| `GET/POST /api/v1/sources` | 查询/登记来源；登记不执行代码 |
| `POST /api/v1/sources/{id}/reviews` | 提交指定快照审查 |
| `GET/POST /api/v1/strategies` | 策略目录/创建定义 |
| `POST /api/v1/strategies/{id}/versions` | 创建冻结版本，不修改历史版本 |
| `POST /api/v1/strategy-versions/{id}/validate` | 生成 schema/能力/测试作业 |
| `POST /api/v1/experiments` | 回放、参数研究、消融或 OOS 作业 |
| `GET /api/v1/experiments/{id}` | 状态、结果、拒绝/失败和工件 |
| `GET /api/v1/comparisons?run_ids=...` | 比较前核验快照/成本/执行可比性 |
| `POST /api/v1/instances` | 指定策略版本、账户、股票池、预算、运行模式 |
| `POST /api/v1/instances/{id}/commands` | start/pause_entries/exit_only/stop/rollback |
| `POST /api/v1/deployments/{id}/approve` | 显式批准精确版本与限额 |
| `GET /api/v1/signals`、`GET /api/v1/plans` | 区分观察与操作计划、状态/到期 |
| `POST /api/v1/plans/{id}/feedback` | 确认/忽略/取消理由，不造交易 |
| `POST /api/v1/executions/import` | 真实/仿真成交导入和对账 |
| `GET /api/v1/health` | 数据/作业/风险/模型健康 |
| `GET /api/v1/events` | 本地 SSE 实时状态推送，断线可按序号补读 |

所有有副作用命令要求 `Idempotency-Key`，响应返回 command/job ID。长任务返回 202，不在请求中跑完整回测。启停使用实例状态版本的乐观并发检查；状态冲突返回 409，不覆盖新状态。

#### 7.2 CLI 是首要验收入口

预定命令包括 `xquant data validate`、`xquant strategy validate`、`xquant replay`、`xquant experiment compare`、`xquant instance start --mode paper`、`xquant serve`。精确实施见 Codex 文档。命令是目标接口，不是本交付已经安装完成的程序。

### 8. 网页信息架构：策略优先

首页是“策略驾驶台”，不是股票新闻门户。默认展示运行实例、数据截止、风控状态、待处理计划、近期实验和失效策略；任何示例曲线必须带 DEMO 水印。

| 页面 | 主要内容 |
|---|---|
| 策略库 | 策略卡、证据等级、来源、频率、市场、状态、标签、最近验证 |
| 策略详情 | 交易假设、规则、参数 schema、版本差异、相关实验与部署 |
| 实验中心 | 队列/进度/预算、同口径比较、消融、参数稳定性与失败记录 |
| 回放工作台 | K 线、当时可见区域、状态转换、计划/成交、拒绝原因；支持逐棒推进 |
| 部署中心 | 实例/账户/股票池、模式、预算、启停、审批和回滚 |
| 候选雷达 | 全市场过滤漏斗、观察事件、可核对计划，不能把分数写成胜率 |
| 持仓与风险 | 真实可卖数量、成本、保护阈值、行业敞口、策略归因与手工仓位 |
| 预测管理 | 模型目标、训练/校准截止、样本外指标、适用范围、漂移/失效 |
| 数据中心 | 覆盖率、延迟、权限、缺失、修订、配额、熔断与来源差异 |
| 通知/复盘 | 投递、用户反馈、成交导入、计划执行差异、逐笔复盘 |

提供“为什么没有交易”入口，展示每一层的排除数量和示例，避免 Codex 或用户通过随意降低所有门槛来制造信号。完整策略编辑器可以后续做，首版采用 schema 表单和 YAML 导入即可。

### 9. 数据获取与限流：服务策略，不让策略直接抓网站

全市场日线集中批量同步；持仓优先，研究历史回补次之；盘中订阅仅在授权与预算允许时开启。每个策略不得各自重复拉同一股票数据。

DataHub 负责供应商/凭证/接口/主机多层统一配额、请求合并、本地缓存、增量水位、条件请求、最大并发、429 的 Retry-After、指数退避和抖动、熔断及质量隔离。配额来自实际合同/官方文档配置，不在策略里写死“每秒可以请求多少”。

403/验证码/授权失败时暂停相关源，检查权限；不使用轮换账号、代理池、验证码绕过或伪装来规避限制。备用数据源必须也被授权，且完成价格/复权/时间戳一致性检查；切源生成新 revision，不把不同口径拼成同一条事实序列。

无外网时可研究已持有的授权快照，但不能把上周数据当今天信号。恢复联网后补数据→校验→重建必要状态→判断计划时效；过期历史信号只归档，不能积压后一口气发送为当前买入提醒。

### 10. 通知、休眠与本地可靠性

独立 outbox 保存业务消息与有效期。优先支持站内、飞书 webhook、邮件；其他聊天工具通过适配器扩展，不依赖个人聊天软件非官方协议。首版选择用户实际配置的一个外部渠道即可。

去重键建议 `(instance_id,instrument_id,setup_id,state_transition,event_session)`。同一形态反复轮询不能重复轰炸；首次观察、确认、取消、退出、数据失效是不同事件。过期的买入提醒不再投递，未投递的退出/系统风险按最新状态重新生成摘要，不能无声丢弃。

消息展示策略版本、数据时点、观察/操作性质、价格范围、数量上限、保护阈值、目标、执行窗口与失效条件。默认不向第三方群聊发送账户总资产、成本明细或 API 密钥。需要外发具体持仓时要求用户明确设置允许字段。

电脑休眠/退出程序时服务不会运行，这是本地产品能力边界。界面必须显著显示最后健康心跳和最后成功数据时间；系统恢复后应提示监控空窗。需要持续监控时，应部署到持续运行的本地设备/自托管主机，不将个人电脑关闭后的消息能力写成保证。

### 11. 安全与来源代码执行

默认仅监听 `127.0.0.1`，严格校验 Host/Origin、会话令牌与有副作用请求的 CSRF。首次本机配对建立本地登录会话；不将本地服务默认开放公网或局域网。Web 静态目录不能映射行情、密钥、账户数据库或整个用户主目录。

仓库 URL 的获取端需防 SSRF/路径穿越，仅允许预设外部源，不接受随意访问私网地址。用户上传策略包默认静态分析，禁自动执行 setup.py/安装钩子。Python 子进程不是安全沙箱；首版只执行本项目内置和经过人工审查的策略。后续第三方不受信任代码必须采用 OS/容器级限制、无网络、最小只读挂载、CPU/内存/时间限制，仍不得授予账户与密钥权限。

密钥使用系统安全存储或受限配置文件，不能进入 git、日志、报告或模型提示词。备份需包含 SQLite 一致性快照、相关 manifest/工件与版本，并定期执行恢复测试；只复制一个正在写入的数据库文件不算完整备份策略。

### 12. 容错、性能与可观测性

实时持仓检查、通知、批量扫描、研究训练采用独立优先级预算。默认研究并发 1，最大并发由本机资源配置；不得因为跑参数搜索占满内存而让风险服务无响应。数据量相关性能目标在本机基准测试后填写，不声称所有设备都能在固定秒数扫完全部股票。

关键观测项：最后完成会话、数据缺失比例、队列等待、任务 heartbeat、超时/取消、计划到期量、消息投递延迟、资金预留总额、账本对账差异、每策略过滤漏斗与信号频率异常。指标异常可自动暂停新增，但不要把“停止提醒”当作降低真实持仓风险。

任务执行采用可重试、可重复投递模型；通过幂等业务事件保证不会双记成交、双冻结资金或重复创建同一计划。进程在计算完成前崩溃可重算，落库事务成功但应答丢失时重复请求返回原结果。

### 13. 非目标与禁止捷径

首版不做全市场高频交易，不做保证收益排名，不靠大模型自主改策略上线，不默认加载所有开源框架，不允许未经许可抓取，不使用一套未含成本的指标回测作为实战证明。

禁止完成一个充满示例数据的仪表盘后宣称“量化系统完成”。真正的首个里程碑是策略内核和回放验证；网页只在此之后展示已存在、可追溯的结果。

---

## 第四篇：Codex 编码顺序与验收

*原分篇文件：`04_CODEX_IMPLEMENTATION.md`。*


> 将整个交付目录放到项目 `docs/strategy_v2/`。  
> 本文件规定执行顺序；交易细节以 `02_SRPA_STRATEGY_SPEC.md` 为准；产品/架构以 `03_STRATEGY_PLATFORM_DESIGN.md` 为准；上游来源与审查以 `01_SOURCE_AUDIT.md` 为准。  
> 本交付是设计与策略规格，不包含已经实现的引擎。下列 CLI 为要求实现的目标命令，不应向用户声称现在已经存在。

### 1. 可直接发送给 Codex 的启动指令

```text
你正在开发 X-Quant：本地、策略优先的量化研究与策略管理系统。

先完整阅读 docs/strategy_v2/ 下的 01_SOURCE_AUDIT.md、
02_SRPA_STRATEGY_SPEC.md、03_STRATEGY_PLATFORM_DESIGN.md 和本文件，
以及 configs 下两个 YAML。旧 X-Stock 设计中“先网站后策略”的顺序已被替代。

本轮目标是 P0+P1：实现可测试的量化内核与本地回放最小闭环。
不要先搭建大型网页、新闻模块或 LLM 多代理；不要只生成接口空壳。

先检查现有仓库，保留已有可复用代码、数据与用户修改，不进行破坏性重建。
建立 docs/strategy_v2/IMPLEMENTATION_STATUS.md，逐项列出已完成、未完成和阻塞项。

1. 固定两个用户指定上游仓库的真实 commit SHA，审查列出的关键路径、许可与差异。
   无法访问源时标记 SOURCE_PIN_PENDING；继续独立实现合成数据可验证内核，
   但不复制未知许可代码、不伪造 SHA、不声称上游已复现。
2. 实现严格时间切片、市场日历接口、OHLC 校验、EMA/ATR/RS、确认枢轴、SR 区域。
3. 实现 S-BR 的有限状态机、入场/失效/退出、计划有效期、风险数量与现金预留。
4. 实现 B-CASH、B-EMA、B-DC 基线与共用的下一会话参考执行/账本。
5. 使用合成夹具建立必经测试，特别是未来数据、同棒交易、H2 计数、
   成本/数量、跳空不成交、重复事件和恢复后不得重复计划。
6. 实现 CLI 导入/校验本地行情、校验策略配置、运行回放、导出报告。
7. 测试与报告必须真实生成；合成数据显著标 DEMO，不能伪造真实策略收益。

量化策略代码必须独立于 FastAPI/React/外部行情库，研究模式禁止联网，
真实成交只能由 execution import 进入真实账本。
core 使用 Python，默认本地 SQLite+Parquet，不预设必须有 Redis 或云数据库。
不要一次引入多个完整回测框架。

完成后提供：修改文件、可复现命令、真实测试结果、合成回放工件位置、
关键设计决定、未完成的收益验证/数据/许可事项。
未经明确审批不得启用 live_assist，更不得自动券商下单。
```

### 2. 仓库结构目标

```text
x-quant/
├── pyproject.toml
├── uv.lock
├── src/xquant/
│   ├── domain/           # 时间、证券、事件、计划、状态、Money
│   ├── marketdata/       # Provider、PIT 快照、公司行为、交易规则
│   ├── features/         # EMA/ATR/RS 与只读时间边界
│   ├── levels/           # xq_sr_v01、冻结与 rebase
│   ├── regimes/          # 个股状态与基准闸门
│   ├── strategies/       # S-BR、基线；S-H2/S-RG 后续
│   ├── portfolio/        # 预算、预留、账本、lot、RiskGuardian
│   ├── execution/        # 参考回放、仿真、真实成交导入
│   ├── research/         # 实验、purge、消融、评估、模型工件
│   ├── registry/         # 来源/策略版本/模型/审批
│   ├── runtime/          # 协调器、调度、任务、事件恢复
│   ├── notifications/    # outbox、适配器、反馈
│   ├── api/              # 薄 API，不含策略公式
│   └── cli.py
├── web/                  # P3 再开发 React 管理网站
├── migrations/
├── tests/
│   ├── unit/
│   ├── property/
│   ├── integration/
│   ├── acceptance/
│   └── fixtures/synthetic/
├── configs/              # 已校验的策略/市场/费用/运行配置
├── docs/strategy_v2/
└── artifacts/            # 默认 gitignore；含输入指纹和运行报告
```

依赖锁文件必须由实际解析生成，不能手写虚假包哈希。Python/Node 版本取实际依赖兼容集合后固定，不在代码中自动跟随 latest。运行路径支持 Windows/macOS/Linux 的普通本地目录，不继承上游个人 Windows 路径。

### 3. P0：把策略和数据边界固定下来

交付 `sources.lock.json`、来源审查结果、输入 schema、策略 manifest/schema、核心领域类型、模拟市场规则/费用夹具与合成行情生成器。对本项目自写代码和上游适配代码做明确区分。

`sources.lock.json` 中真实 SHA 未取得则必须为 null 且状态 pending；不准用日期、分支名或随机哈希代替。已固定后保存关键源文件哈希、许可、抓取时间、检查文件列表。上游下载目录只读，不自动安装/执行。

数据契约必须包含 `available_at`；市场规则至少有 tick、买入数量规则、卖出可用时间、交易时段、价格限制、费用与生效日期。示例市场规则只能用于 DEMO，不宣称代表所有 A 股板块或当前真实费率。

**P0 验收**：配置无法通过未定义字段、非法参数、错误时间顺序；价格低于零、OHLC 不一致、重复会话和股票池缺关键数据能被明确识别。缺少正式规则/费用配置不阻塞合成研究，但阻止正式部署。

### 4. P1：交付首个能运行的策略内核

实现 xq_sr_v01、状态分类、S-BR、共同风险与 EOD 回放、B-CASH/B-EMA/B-DC。公开接口实现后必须有实际算法，禁止以 return [] 之类占位代码通过演示。

状态、事件、计划和账户写入路径只允许 Coordinator 提交。策略计算不读取系统当前时刻、不联网、不发通知、不写真实账本。Context 必须包含事件类型、只读市场切片、状态、实际/仿真持仓视图，并能处理填单/取消/公司行为对状态的影响。

最少设计四组合成行情：有效突破回测、突破后立即失败、仅影线触及不确认、确认后次一会话跳空越界。至少有一组真实走完生成计划→参考成交→退出→成本→权益变化，预期事件日期和数值必须由独立手算/小样本推导断言。

目标命令示例：

```bash
uv sync --locked
uv run pytest tests/unit tests/property tests/integration -q
uv run xquant data validate --input tests/fixtures/synthetic/valid_case.parquet --mode demo
uv run xquant strategy validate --config configs/demo/srpa_demo.yaml
uv run xquant replay --config configs/demo/srpa_demo.yaml --output artifacts/demo/srpa
uv run xquant experiment compare --runs artifacts/demo/srpa artifacts/demo/donchian
```

CLI 应优先实现 clear errors、非零失败退出码与 JSON 日志，不要求固定依赖某个 Make 工具。回放报告中模拟曲线、样本数量和缺乏真实验证必须直接可见。

**P1 验收**：不启动网页、不提供任何外部密钥，也能完整运行；同一输入在固定环境重跑得到一致事件和成交结果；没有真实数据时不生成“真实回测优于基准”的结论。

### 5. P2：把研究做成可复现的策略管理能力

实现策略目录、版本冻结、参数 schema、实验记录、批量回放、基线比较、逐层过滤漏斗、时间外切分/purge、标签成熟、成本与跳空压力测试，以及报告导出。数据接入至少支持本地 CSV/Parquet 与一个实际取得权限的行情 Provider；权限未取得时保留 pending，不捏造接口成功。

研究脚本必须复用策略引擎，不能另写一套简化收益曲线把风控、停牌和未成交删掉。保存所有实验尝试与失败原因；参数搜索预算≤24 组作为首轮默认。先验证主策略规则层，不要求在此阶段引入 LightGBM。

**P2 验收**：实验可以从指纹定位到数据、规则、版本和执行模型；改变后续行情不改变之前特征/信号；同日不同证券不跨训练/验证分割；窗口未完整结束的标签不进入训练。缺少授权历史/点时信息时只能标记研究受限，不允许自动晋级 OOS_PASSED。

### 6. P3：完成本地策略管理网站与仿真闭环

此时才实现网页。页面必须读取真实注册表/实验/计划 API，不在前端写策略公式。实现策略卡片、参数/版本 diff、回测记录、K 线逐棒回放、实例绑定/启停、持仓/风险、候选雷达、数据健康和通知反馈。

实现 `paper` 运行、策略计划 outbox、一个真实配置的外部通知渠道、实际成交 CSV 导入与幂等对账。程序退出/休眠恢复不得发送过期买入；暂停新增必须继续持仓风险监控。删除或回滚版本不得导致已有 lot 无退出政策。

**P3 验收**：用户能在浏览器创建/复制策略版本、启动一次回放、查看失败原因、绑定仿真实例、收到正确标识的消息、确认/忽略、导入成交并查看复盘。该闭环与 CLI 使用同一服务层。

### 7. P4：增加预测与更多策略，而不是先上 AI

实现 P-TOUCH、P-REACT（包含 timeout）、P-TRADE 的标签与模型注册；先发生率/逻辑回归，再做 LightGBM 挑战者。加入 H2 与偏多区间独立策略、动量基线、Boll/CCI 挑战者、模型校准/漂移、Champion/Challenger 和归因。

LLM 仅允许解释与研究假设，必须标为旁路，不得调用账户写接口、不修改硬风控、不得把未经统计评估的百分比发成获利概率。

**P4 验收**：模型缺失/特征版本错配/标签版本变化时拒绝统计过滤；同一股票上的概率必须注明目标、时点、样本与校准版本；概率指标提升与交易收益提升分别报告。

### 8. P5：批准人工实战辅助，保留证据闭环

落实版本审批、用户风险限额、实际可执行计划对照、通知延迟/休眠空窗验证、备份恢复、压力情景和紧急暂停。只有满足规则书门槛的精确版本才可进入 `live_assist`。

未通过收益/执行证据时，交付“功能可用但策略未达资金部署条件”的系统，不要求 Codex 为通过验收编造赚钱曲线。自动券商交易不是本阶段的隐含任务。

### 9. 必经验收测试

| ID | 测试 | 必须观察到的结果 |
|---|---|---|
| T01 | 行情前缀计算与完整序列按时点计算 | 同一时点特征、区域、状态与信号一致 |
| T02 | 任意替换 t 之后行情 | t 及以前特征/信号不变；未来标签可以变化 |
| T03 | 分形枢轴确认 | i+2 完成前不能使用 i 的枢轴 |
| T04 | 区域冻结后追加新高低点 | 旧计划的区域 ID/边界不被重新画图覆盖 |
| T05 | 成交量分配 | 各箱总量等于输入衰减量，含零幅棒 |
| T06 | H2 两腿计数 | 连续上涨或同一回撤不冒充 H2；每腿可定位 |
| T07 | S-BR 阶段顺序 | 突破当棒不能完成整个状态机并成交 |
| T08 | 次一会话入场 | s 收盘信号的参考成交时间严格晚于 s |
| T09 | 下一会话跳空超上限/低于有效区 | 取消计划，不调宽入场区间 |
| T10 | EOD 盘中穿目标/保护阈值但收盘未穿 | 不记为已止盈/止损；记录日内风险观察 |
| T11 | 跟踪阈值顺序 | 先用旧阈值判断，再计算下个会话的阈值 |
| T12 | T+1/卖出数量限制夹具 | 不允许出售尚不可卖数量，不虚构保护成交 |
| T13 | 停牌/开盘限制价缺排队信息 | 保守不成交，留下原因而不是删除交易 |
| T14 | 最低佣金和小单 | 整手规范化后重新计费，超预算时减量或拒绝 |
| T15 | 多个实例同时触发 | 账户预算只被分配一次，退出优先，确定性排序 |
| T16 | 同股多策略冲突 | 不叠加重复仓位；保留次要命中依据 |
| T17 | 未分配手工仓位 | 占用风险/现金/行业敞口，历史收益不归新策略 |
| T18 | 重复导入同一成交 | 账本与持仓只更新一次 |
| T19 | 通知成功/用户确认但无成交 | 真实仓位保持不变 |
| T20 | 重复事件、任务重试、事务后应答丢失 | 无重复计划/资金预留/成交 |
| T21 | 休眠后补齐 5 天行情 | 旧买入计划过期；退出风险按当前状态重建 |
| T22 | 暂停新增/回滚 | 已有仓位仍有风险监控，原版本血缘保持 |
| T23 | 未来 40+10 会话窗口未成熟 | 标 censored，不计 no_touch 或 timeout |
| T24 | 反应标签未决 | 成熟 timeout 进入条件分布，不被偷偷丢弃 |
| T25 | 区域无预测输出 | 几何召回计入漏检，不能提高评分 |
| T26 | 多标的时间切分与 purge | 同日不跨组，重叠标签区间被清除 |
| T27 | 测试集访问审计 | 调参后不能仍标同一保留段未被查看 |
| T28 | 模型特征顺序/版本/频率错配 | 拒绝推断，不仅打印 warning 继续交易 |
| T29 | `p_stall` / SR 分数 | 不作为百分比或交易获利概率展示 |
| T30 | 缺关键测试数据 | skip 单独显示，不计作通过，不允许晋级 |
| T31 | 配置缺市场/费用/基准/风险审批 | research demo 可单独运行，live_assist 不可启动 |
| T32 | Python 策略试图联网/读密钥 | 核心内置约束检测阻止；不信任代码不进入首版运行 |
| T33 | 拆股与现金分红 | 账本数量/现金和价格尺度一致，无虚假突破/收益 |
| T34 | 交易所日历与时区 | 午休/假期/未结束周线不生成错误会话数据 |
| T35 | 备份恢复 | 能恢复同一版本、账本、未结计划与所引用工件 |
| T36 | 没有符合条件的交易 | 正常输出零计划和过滤漏斗，不报成功买入或强行造信号 |

T32 中“子进程”不代表安全沙箱。应先拒绝不信任插件进入执行，后续沙箱能力必须有 OS 级测试，不能仅通过 monkeypatch socket 就宣称隔离完成。

### 10. 每阶段报告格式

每阶段提交 `IMPLEMENTATION_STATUS.md`，分别列出实现功能、实际运行命令、通过/失败/跳过测试、生成的工件、真实数据覆盖、尚未验证事项、下一阶段依赖。任何实际测得的性能/收益必须附 run_id 与数据/参数指纹。

代码评审至少覆盖：时点与价格尺度、指标初始化、标签定义、真实可成交性、费率/数量、预算竞争、状态恢复、版本不可变与许可。网页截图不是量化逻辑验收材料。

### 11. 不允许的完成方式

禁止只搭建前端卡片；禁止把全部开源策略装进来却没有统一接口；禁止使用未来高低点后再给历史入场；禁止忽略不能成交样本；禁止回测用日内止损而实际只有收盘提醒；禁止把“模型产生了概率”当作“模型通过验证”；禁止用真实账户风险承受能力未知的默认值直接部署。

目标不是策略数量最多，而是**每个纳入的策略都能解释、重跑、比较、管理、停止、回滚，并在真实约束下接受检验**。

---

## 第五篇：交付检查与证据边界

*原分篇文件：`05_VALIDATION_NOTES.md`。*


### 已完成的研究工作

已读取两个用户指定仓库的关键策略/预测路径，包括 PA 策略路由、决策/结构特征、价格行为策略文本，以及 SR 检测器、概率模型、标签、训练/评测路径；核查了社区策略、特征/训练工作流、偏差检查与版本管理资料。来源审查文档列有 34 个来源入口。

核查是针对读取到的公开源码/文本的静态审查，未完整 clone、安装或运行上游项目，不能推定未阅读模块的性质。远端可变分支与检索快照的最终一致性，需实施时以固定 SHA 再核对。

### 本地交付检查

使用随包 `check_delivery.py` 检查 Markdown 代码围栏、相对文件链接、来源编号、JSON/Python 示例语法、两份 YAML 可解析性、策略目录唯一性、主要参数/状态时间一致性、安全默认值，以及 36 项待实施验收用例索引。

另独立核对了规则书的仓位/RR 合成算例与一个条件概率算例。检查结果保存于 `delivery_checks.json`。这些是**文档、参数和算术检查，不是量化引擎测试、回测或实盘结果**。

仓位算例为：风险允许 268 股，按示例 100 股单位向下取整为 200 股；预算风险 186 元，净 RR 约 1.94。

条件概率算例假设：1,000 个区域中 200 个接触；接触后 90 个守住、60 个突破、50 个超时。排除超时后守住率是 60%，对所有接触的守住率是 45%。把接触率 20% 直接乘 60% 得到 12%，而无条件守住事件占比实际为 9%。这说明为什么不能忽略条件中的“已判定”，并不代表真实股票事件的概率。

### 尚未完成、不得宣称已完成

本项目策略引擎、正式策略管理网站、上游完整测试复现、用户真实行情上的成本后回测、时间外预测校准、影子部署和实际成交验证均尚未完成。主策略和目录均标记 `NOT_IMPLEMENTED / E1 / default_live_enabled=false`，并保留正式数据/费用/日历/市场规则与来源 SHA 的待填字段。

“策略规则已经可以交给 Codex 编码”与“策略已经证实可盈利”是不同结论。本次交付达到前者，不对后者作未经验证的承诺。

### 重新运行交付检查

安装 PyYAML 后，在本目录运行 `python check_delivery.py`。脚本不访问外网、不执行上游代码、不连接券商或发送通知。后续 Codex 修改规则和配置后应更新对应断言；不得为了使检查通过而把未完成的实现/收益状态改为已完成。

---

